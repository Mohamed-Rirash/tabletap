# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 6 — Identity, Ratings & Analytics — **CLOSED (all 3 features done).** Phase 7 — Platform & Polish — Feature 19 (Subscriptions & Platform Admin) **CLOSED**. **Feature 20 (Notifications & PWA Hardening) in progress**, staged across five commits (same discipline as Features 18/19).

**Feature 20 progress, 1 of 5 planned commits landed:**
1. **Web Push infrastructure** (`4b8b3cb`): `{:web_push_ex, "~> 0.2.0"}` was already a mix dep, unused/unconfigured until now — VAPID keypair generated via `mix web_push_ex.vapid`, public key + subject in `config.exs` (not secret, safe unconditionally), private key per-env (dev/test hardcoded dev-only pair, `runtime.exs` prod reads `WEB_PUSH_VAPID_PRIVATE_KEY`) — same split as the WaafiPay platform credentials (Feature 09/19). New `push_subscriptions` table + `Tabletap.Notifications.PushSubscription` schema — **not tenant-owned** (`skip_org_id: true` throughout, same reasoning `Accounts` already documents for `users`/`user_tokens`: a subscription belongs to a person, who can hold memberships across more than one org). New `Tabletap.Notifications` context: `subscribe/2` (idempotent upsert on `(user_id, endpoint)` — caught a real Ecto gotcha where `on_conflict` without `returning: true` silently returns the *discarded* client-generated id instead of the existing row's, fixed and covered by a test), `unsubscribe/2`, `send_push/2` (a real `WebPushEx.request/2` + `Req.post/2`, deletes the subscription on a `404`/`410` — the standard dead-subscription signal). Test HTTP calls routed through `Req.Test` (Req's own built-in test plug) via a `config :tabletap, :web_push_req_options` seam, same "no test hits a real provider" discipline as `Payments.ProviderMock`, just no Mox behaviour needed for a single function. New service worker (`priv/static/sw.js`, root-served — `TabletapWeb.static_paths/0` gained `sw.js` — a service worker's scope is its own path and below, so it can't live under `/assets`) handles `push`/`notificationclick`. New `push_subscription.js` hook + shared `<.push_subscribe_button>` component wired onto `Waiter.QueueLive` and `Manager.DashboardLive` — the two surfaces Web Push targets (waiter new-order/call, manager low-stock). 824 tests green (`Waiter.QueueLive` had zero test coverage before this — a pre-existing gap flagged, not backfilled beyond the new wiring).

**What's still genuinely missing, in commit order:** 2 — wire push into the 3 real events (waiter new-order, waiter call, manager low-stock — all three already broadcast over PubSub, no new domain events needed) via a fire-and-forget Oban worker, plus Q28's in-app audio alert (Web Audio API oscillator, no binary asset). 3 — PWA manifests per surface + offline shell + install prompts. 4 — the browser-floor gate (Q56, conservative UA sniff) + iOS install-to-home-screen onboarding block (Q28). 5 — the onboarding checklist widget.

<details>
<summary>Feature 19 — Subscriptions & Platform Admin (2026-07-20)</summary>

Staged across five commits:
1. **Feature-gating retrofit** (`465da06`): closed the router's own leftover TODO from Feature 18 — `TabletapWeb.PlanHooks`'s already-built `:report_center`/`:org_comparison` clauses (only `:inventory` was actually wired in) are now applied. `/reports` moved into its own `live_session :manager_report_center` (mirrors `:manager_inventory`'s split-from-`:manager` shape); `/analytics/venues` moved into a new `live_session :owner_org_comparison` stacking `{ScopeHooks, :require_owner}` + `{PlanHooks, :org_comparison}` — wrong role and wrong plan both redirect, for different reasons. New `UserAuth.require_report_center_feature/2` (mirrors `require_inventory_feature/2` exactly) gates `/reports.csv`; `/analytics/revenue.csv`/`/analytics/menu-performance.csv` stay deliberately ungated — pricing.md's feature table only tiers "Full Report Center" and "Cross-venue comparison," not the individual always-available Revenue/Menu-Performance/Feedback/Staff/Customers screens. Nav links in `layouts.ex` for "Reports" and "Org View" gained the same `Plans.feature_enabled?/2` conditional the "Inventory" link already had. First test coverage ever written for this gating mechanism (none existed for `:inventory` either) — trial-always-passes and off-trial-wrong-plan-redirects cases for both newly-gated routes.

**What Commit 1 confirmed was already built** (from the concurrent-session `Plans`/`PlanHooks` scaffolding bundled into Feature 18's Commit 6, now audited against pricing.md and found correct): `orgs.plan`/`subscription_status`/`trial_ends_at` (Feature 01's original migration), `platform_fee_ledger` (Feature 09), `config/plans.exs` + `Tabletap.Plans`'s full API, and — notably — `config/runtime.exs` already has the **platform's own** WaafiPay merchant credentials (`platform_merchant_uid`/etc., separate from any venue's), committed since Feature 09 and explicitly earmarked in its own comment for Q59 billing collection. None of this needed rebuilding.

2. **Billing screen + trial banner + add-venue** (`c692fa6`): new `Manager.BillingLive` (`/settings/billing`, owner-only) shows the current plan/price/fee-rate, a monthly itemized preview (`Payments.unsettled_platform_fees_by_currency/1` — every unsettled `platform_fee_ledger` row **grouped per currency, never summed across them**, same Q53 discipline `Analytics.org_comparison/3` already follows — explicitly labeled "a preview only, no charge happens here" since `Tabletap.Billing`'s actual collection is Commit 3's job), a plan-change form (`Tenants.change_plan/2` — upgrade unrestricted, downgrade blocked while venue count exceeds the target cap, Q48), and the app's first-ever self-serve "add venue" form (`Tenants.create_venue/2`, same `resolve_city/1` city-picker as org signup, blocked at the plan's venue cap). `DashboardLive`'s old always-on trial badge was promoted into a shared `<.trial_banner org={}>` in `CoreComponents` (small badge past day 4-remaining, a prominent "choose a plan" alert banner from day 10 onward per pricing.md's own "countdown from day 10"), reused on both `DashboardLive` and `BillingLive`.

3. **Billing engine** (`56aed9f`): new `invoices` table (one row per org per billing period, inserted `pending` *before* the wallet charge — same idempotency shape `Payments.Payment`'s own `pending` row plays, with a unique `(org_id, period_start)` index as the actual double-collect guard) + `orgs.billing_wallet_msisdn`; `platform_fee_ledger.invoice_id` upgraded from a bare uuid placeholder to a real composite FK now that `invoices` exists. New `Tabletap.Billing` context: a fixed **30-day billing period** (no calendar-month arithmetic in this codebase), billed **in advance** starting the day the trial ends; `collect_invoice/1` pushes one `WaafiPay.charge/2` call against the platform's own credentials (new `Payments.platform_credentials/0`) for plan price + same-currency unsettled fees, settles matching ledger rows on success, and drives `subscription_status` (`:active` on success; first miss → `:past_due` — architecture.md is explicit that ordering keeps working during this grace; a **second consecutive** miss → `:canceled`, a reasoned two-strikes interpretation since no explicit grace-day count exists anywhere in design-qa.md). `Billing.expire_unpaid_trial/1` converts a trial that ran out with no wallet on file straight to `:canceled` (Q29 — no failed attempt to grade as `past_due` when none was ever made). New nightly `Billing.Workers.CollectMonthlyInvoices` (cross-tenant loop, fixed off-peak UTC cron — same "good enough" simplification `DailyRollup`'s own cron comment already established for Q40's cutoff-timing ask, since this is an org-wide not venue-wide transition). `CoreComponents.trial_banner/1` evolved into `subscription_banner/1` (trialing/past_due/canceled, all four states) and moved into `Layouts.manager` itself so every back-office page shows it automatically, not just Dashboard/Billing — `DashboardLive`/`BillingLive`'s own explicit calls were removed as redundant. `Public.MenuLive` gained a dedicated `:unavailable` render clause (design-qa.md Q29's exact wording) for a canceled org's venue — a full replacement page, not just another banner over a still-browsable menu like `:paused`/`:closed`. `BillingLive` gained the actual wallet-number form (`Tenants.set_billing_wallet/2`) needed to make the whole engine reachable.

4. **Platform admin panel** (`999da23`): `users.platform_admin` boolean (migration; "us only," no self-serve path — set directly, never through any UI this app exposes). New `TabletapWeb.AdminAuth` on_mount, distinct from `ScopeHooks` — an admin isn't a member of any tenant, so there's no `scope.role` to check, just `current_scope.user.platform_admin` directly. New `Tabletap.Admin` context — `skip_org_id: true` throughout (one of the few contexts code-standards.md's "Tenancy Rules" allows this, alongside `Accounts`/`Tenants`): `list_tenants/0` (plan, subscription_status, venue count, order count, per org), `get_tenant/1`, `cash_share_by_venue/1` (design-qa.md Q24 — cash payments vs every succeeded payment, per venue, so systematic cash-steering is at least visible), `list_invoices/1`. New `Admin.TenantsLive` (`/admin`) + `Admin.TenantLive` (`/admin/tenants/:id`), both using the plain `Layouts.app` shell (not `Layouts.manager` — an admin has no org/venue to drive that sidebar). Strictly **read-only end to end** — build-plan.md's own "impersonation guard (read-only)" is interpreted as "an admin can look at a tenant's data through this panel, never act as that tenant," not a session-swap/sudo mechanism; there is no write path anywhere in `Tabletap.Admin` or its two LiveViews.

5. **Data lifecycle (GDPR) + downgrade enforcement** (`257c703`): `Accounts.delete_account/1` (design-qa.md Q15) — deleting the `User` row is the entire mechanism, since `orders.customer_user_id`/`item_ratings.customer_user_id` already carry `on_delete: :nilify_all` from their own Feature 16/17 migrations (anticipating exactly this); refuses to delete an account holding any staff membership (`memberships.user_id` is `on_delete: :delete_all` — cascading through it would orphan a venue's staff/owner). Wired into `UserLive.Settings` as a "Danger zone" section, gated behind the page's existing sudo-mode requirement. Tenant offboarding (Q15/Q31/Q54): `Tenants.initiate_offboarding/1` (owner-only, idempotent) + `Tenants.export_org_data/1` (menu/orders/ingredients CSVs, zipped by a new `Manager.OrgExportController`, owner-only) both live in `BillingLive`'s new danger zone. New `Tabletap.Offboarding` context implements the 90/180-day retention **not** by surgically deleting two dozen child tables while keeping an org half-alive — instead, at the 90-day mark it copies out everything worth keeping (Q31's anonymized customer order archive, Q54's flat payment/dispute-evidence snapshot with its own `retain_until`) into two new denormalized tables with no FK back to the org, then hard-deletes the **entire org in one step**, letting the schema's own existing cascade do the rest. A nightly `Offboarding.Workers.PurgeOffboardedTenants` finds orgs 90+ days into offboarding and purges dispute records past their 180-day mark. Downgrade-side venue-cap enforcement (Q48) turned out to already be fully covered by Commit 2's `Tenants.change_plan/2` — nothing more to add there. 814 tests green across the whole feature.

</details>

<details>
<summary>Feature 18 — Analytics Dashboard & Rollups (2026-07-19 to 2026-07-20)</summary>

Staged across six commits (by far the largest feature in the plan — 7 dashboard screens + org comparison + a 13-report Report Center):
1. **Foundation** (`97febd0`): `daily_rollups` migration/schema, new `Tabletap.Analytics` context — `compute_rollup/2` mirrors `Payments.z_report_preview/2`'s own business-day-windowing shape; `Analytics.Workers.DailyRollup` (nightly, `queue: :rollups`) recomputes the **last 7 business days** per venue rather than threading an explicit recompute trigger through every `Ordering`/`Payments` write path — a documented interpretation of Q37/Q38; `recompute_count` flags a day whose numbers moved since they first appeared, since rollups (unlike `Payments.ZReport`) are never a closed document.
2. **Screen 1 — Today** (`2ad60c1`): `Manager.DashboardLive` gained the full live tile grid, a floor strip, and an alert feed, all reading new `Analytics.today_summary/1`/`today_operations/1`/`today_alerts/1` — `today_summary/1` is just `compute_rollup/2` called on today's still-open business day, so Today and the eventual rollup for the same day can never disagree.
3. **Revenue & Sales + Menu Performance** (`024e181`): `Analytics.range_summary/3` (gap-free daily series, stored rollups + today live), `previous_period_range/2`, discount/comp/refund breakdowns, platform fees, hourly totals; `menu_performance/3` + `menu_quadrant/1` (BCG-style stars/plowhorses/puzzles/dogs) + `category_mix/3`. New `Manager.Analytics.RevenueLive`/`MenuPerformanceLive`, each with a date-range picker and CSV export.
4a. **Rich Feedback + Customers** (`738e064`): `Manager.FeedbackLive` (Feature 17's simple list) gained a trend, 1-5 distribution, rating rate, worst/best-first per-item list, per-waiter averages (always shown with the venue average and hours worked — a fairness guardrail, never a naked leaderboard), and the low-rating alert. New `Manager.Analytics.CustomersLive`: new vs returning, visit-frequency histogram, 30-day repeat rate, top spenders (account holders only, privacy-safe). Also fixed a pre-existing flaky assertion in Feature 17's own `feedback_test.exs`.
4b. **Staff & Work + Inventory & Cost** (`0628638`): new `Analytics.staff_summary/3` (per-waiter accept/serve timing read live from orders, not averaged-from-rollup-averages — a deliberate "no average of averages" call — alongside rollup-cached `hours_on_shift`, safe to sum) and `Analytics.inventory_cost_summary/3` (stock on hand, restocks, wastage, stocktake variance, food-cost %). New `Manager.Analytics.StaffLive`/`InventoryCostLive`.
5. **Org View** (`e15a467`): owner-only `Manager.Analytics.VenueComparisonLive` (`/analytics/venues`), `Analytics.org_comparison/3`/`org_totals/1` — per-venue rows only, **never summed across venues** (design-qa.md Q53: a Pro-tier org can run venues in different currencies).
6. **Report Center** (`3fc68b4`): `Tabletap.Analytics.Reports` — 13 report functions (revenue, orders, successful orders & bills, payments, cashier daily cash, assisted orders, inventory, menu performance, feedback, employee work, customers, day-close, profit), every one reusing an existing `Analytics`/Screen 2-7 function rather than a parallel query (owner-dashboard.md's own rule: "no report may compute a number differently than its dashboard twin"). New `Manager.Analytics.ReportsLive` (`/reports`) + `Reports.Csv` (shared CSV rendering, `/reports.csv`) + scheduled email delivery: `report_subscriptions` table, `Analytics.Workers.SendScheduledReports` (nightly, `queue: :notifications`, cron `0 3 * * *` — trails `DailyRollup`'s `0 2 * * *` since reports read rollups for "yesterday"), re-checking each subscription's membership `active`/`role` **fresh at send time** (Q52) rather than eagerly purging on deactivation — nothing in this codebase deactivates a membership yet (`Staffing.force_end_shift/2` is unused in `lib/`), so there's no eager-purge hook to wire up until that feature exists. Two documented gaps, neither built: "manual serve-confirm count" (no persisted column, only a transient telemetry event) and a dedicated season/quarter grouping UI (`same_period_last_year_range/2` covers the actual "this June vs last June" ask).

Design decisions carried across all of Feature 18 (see `Tabletap.Analytics`'s own moduledoc for the full reasoning): payment mix groups by `Payment.provider` only, not the finer ZAAD/EVC Plus/Sahal/WAAFI split owner-dashboard.md asks for (no wallet-network column exists in the schema); `items_sold`'s per-item food cost is a recipe-base estimate, while the rollup's own top-level `food_cost` is the accurate `stock_movements`-derived figure; `staff_metrics` skips escalation rate (no persisted escalation counter exists, only a transient telemetry event) and keys kitchen prep time venue-wide, not per-person (no `kitchen_membership_id` column). Every commit passed `mix precommit`/`mix test` clean (747 tests green at Commit 6). No browser-based verification — no Playwright/`chromium-cli` available in this environment (same documented gap as Feature 02); the LiveView test suite (`Phoenix.LiveViewTest`, real endpoint pipeline) is the substitute, including a loop rendering all 13 report types and a subscribe/unsubscribe round-trip.

</details>

<details>
<summary>Feature 17 — Ratings (2026-07-19)</summary> A new `Tabletap.Feedback` context (architecture.md's own documented `feedback/` module) backed by a single `item_ratings` table — `org_id`/`venue_id`/`order_item_id` (unique, the real one-rating-per-item enforcement)/`menu_item_id`/`customer_user_id` (nilify-on-delete, since `Customer data is NOT tenant-owned`)/`stars`/`comment`, `stars BETWEEN 1 AND 5` as a DB check constraint, not just changeset validation. `rate_item/5` gates on `order.status in [:served, :closed]` — deliberately **not** `:refunded`, since a refund is reachable from several pre-serve statuses too and can't by itself distinguish "served then refunded" from "refunded before the kitchen ever touched it" — inserts, and translates the unique-constraint hit into `{:error, :already_rated}` rather than a raw changeset error the caller would have to pattern-match against. Aggregates (`ratings_summary_for_items/2`, `avg`/`count` grouped by `menu_item_id`) are always live-computed, never cached on `menu_items` — this venue's rating volume is small enough that a grouped query on read costs nothing, and a cached column is one more place to drift.

**Three surfaces, one new PubSub topic** (`"venue:<id>:ratings"` — deliberately separate from `"venue:<id>:menu"` even though a rating and a menu edit both end up reloading similar-looking assigns on the public menu; they're unrelated concerns that happen to look alike). `Public.OrderTrackerLive` grew a per-item `rating_widget` shown once `@order.status in [:served, :closed]`: tapping a star only stages the pick locally (`select_stars`, a plain assign — no write), the comment box only appears once something's staged, and the actual `Feedback.rate_item/5` call happens on explicit Submit — reconsidering a star pick before submitting never creates a stray row that would then block the real rating via the DB's own unique index. `Public.MenuLive`'s `item_card` gained an optional `★ avg (count)` line, computed once per mount from the same `Catalog.list_public_menu/1` result already in hand (no second query), and refreshed live on `{:rating_submitted, menu_item_id}`. `Manager.FeedbackLive` (new, `/feedback`, inside the existing `:manager` live_session) is a deliberately simple newest-first list — item name, order number, stars, optional comment — with trend lines, per-item/per-waiter breakdowns, and low-rating alerts (owner-dashboard.md's much richer "Screen 4 — Feedback") left to Feature 18, not built here.

**647/647 green** (`test/tabletap/feedback_test.exs`: rate/served-gate/already-rated/stars-range/broadcast, both read paths + tenant isolation; `order_tracker_live_test.exs`: widget visibility gating, stage-then-submit flow, live appearance on a same-connection status transition; `menu_live_test.exs`: no-ratings-yet silence, live avg/count update across two raters; new `manager/feedback_live_test.exs`: empty state, rendering, live update, access gating), `credo --strict` clean (one nesting-depth finding in `rate_item/5` fixed by extracting `translate_insert_error/1`, one string-literal-quoting finding fixed with a `~s` sigil), `format` clean. Verified against a real dev server via Playwright, full stack, no shortcuts: signed up a fresh owner → built a category/item → enabled pay-at-counter → a phone-width guest context ordered and paid cash → owner verified the cash payment at `/pos`, ran it through `/kitchen` (Start, Ready), then manual-served it from `/orders` → with the manager `/feedback` page and a separate guest's `/venues/:slug/menu` tab both already open (proving live, not just correct-on-load), the served guest's tracker showed the rating widget, was rated 4 stars with a comment, and **both other tabs updated with zero reload** — feedback list showed the comment instantly, the menu's star line went from nothing to "4.0 (1)" — exactly build-plan's own verify step. Screenshots captured for all three surfaces. Zero console errors from any Feature 17 surface; four unrelated `unknown hook found for ".Reorder"` warnings appeared only while visiting `Manager.MenuLive` for setup (Feature 04's pre-existing drag-to-reorder colocated hook, untouched by this feature).

</details>

<details>
<summary>Feature 16 — Customer Accounts & History (2026-07-19)</summary> `orders.customer_user_id` (deferred since Feature 08's own moduledoc; `carts.customer_user_id` stays deferred — no real caller for it yet, matching the codebase's own established discipline of not adding columns speculatively) lands with two cross-tenant functions in `Ordering`, both looping `Tenants.list_org_ids/0` + `Repo.put_org_id/1` per org — the exact shape `Workers.SweepAbandonedCarts` already established, never `skip_org_id: true` (`Ordering` isn't on that exception list): `link_guest_orders_to_customer/2` (the write — stamps `customer_user_id` on every order across every org matching one `guest_token` in one pass, so "ordered twice then signed up" catches both without a second signup) and `list_orders_for_customer/1` (the `/me/history` read, excluding orders that never really happened — same `pending_payment`/`cancelled`/`expired` exclusion Feature 15's Z-report already uses — but including `refunded`, since food was still made).

**The "Save your history" flow** (design-qa.md's own described sequence) lives entirely in existing surfaces, no new page for the ask itself: `Public.OrderTrackerLive` gained an inline `signup_prompt` component (shown once an order leaves `pending_payment`, hidden again once `order.customer_user_id` is set) that find-or-registers a `User` by email (`Accounts.register_user/1` — already the generic no-password magic-link path every non-owner role uses) and sends a magic link with `guest_token` appended as a query param, mirroring `UserLive.Login`'s own `deliver_login_instructions` call exactly (same `RateLimiter` non-enumeration shape, Q47). `UserLive.Confirmation` — every role's shared magic-link landing page — reads that `guest_token` param and calls `link_guest_orders_to_customer/2` the moment the token resolves, *before* the "Confirm" button is even tapped: reaching the page with a valid, unexpired token already proves email ownership, so there's no reason to gate the link on one more click. The guest_token-in-URL trust boundary is deliberately not a new one — knowing a `guest_token` already grants full access to that order's own tracker with zero auth (Q13's whole design), so carrying it through a magic link *we* generated and *we* emailed only to the address the visitor typed introduces nothing new.

**`/me/history`** (`UserLive.History`, `lib/tabletap_web/live/user_live/` — matching where `/users/settings` already lives, not architecture.md's aspirational `live/customer/` tree the rest of this codebase never actually adopted) sits in the pre-existing `:require_authenticated_user` live_session, no role gate — any authenticated user, staff or customer, sees their own cross-venue history (a manager who also orders elsewhere as a person is still a person). Three sections: monthly spend (grouped by `{month, currency}`, never summed across currencies — design-qa.md Q60, since a customer can order at both a USD Somali venue and an ETB Jigjiga venue in the same month), per-venue totals (safe to sum within one venue — Q53 locks currency per venue), and the flat order list linking out to each order's own tracker. `signed_in_path/1` finally resolves a comment that had anticipated this exact moment since Feature 03/10: a bare `%Scope{role: nil}` — always a customer account today, no other path produces one — now lands on `/me/history` instead of the marketing homepage; **10 pre-existing tests across `user_auth_test.exs`/`user_session_controller_test.exs`/`login_test.exs`/`confirmation_test.exs` had encoded the old `/` fallback as their own expected behavior and needed updating** (one test's title literally was "redirects to / for a user with no staff membership" — retitled to describe the new, correct behavior rather than just patching the assertion).

**One cosmetic nuance, not a bug, worth knowing:** the monthly-spend row has no single venue to derive a display locale from (it can span venues), so it falls back to the app default (`:en`, config.exs) — a "so"-locale venue's own per-venue-total row renders `"US$ 7.00"` while the combined monthly row for the same money renders `"$9.00"`, on the same page. There's no objectively correct locale for a cross-venue, cross-currency-safe aggregate, so this was a deliberate call, not deferred.

**627/627 green** (`test/tabletap/ordering/customer_history_test.exs`: cross-org linking + idempotency + tenant isolation, cross-venue read + status exclusion + isolation between customers; `test/tabletap_web/live/user_live/confirmation_test.exs`: guest_token-param linking end to end, no-param no-op; `test/tabletap_web/live/public/order_tracker_live_test.exs`: prompt visibility across order status and linked state, find-or-register on submit; `test/tabletap_web/live/user_live/history_test.exs`: cross-venue rendering, per-currency/per-venue totals, empty state, cross-customer isolation, any-authenticated-user access), `credo --strict` clean, `format` clean. Verified against a real dev server via Playwright end to end, driving the actual new UI (order placement itself reused Feature 08's already-verified checkout, not re-proven here): two orders at one venue + one at a second venue under one `guest_token` → signed up from the tracker → magic link retrieved from `/dev/mailbox` (confirmed it carries the right `guest_token`) → confirmed → landed on `/me/history` showing both venues, all 3 orders, and totals that reconcile exactly ($4+$4 at venue A, $3 at venue B, $11 combined) → revisited the now-linked order's tracker and confirmed the signup prompt correctly disappeared. Zero console errors. One real timing bug caught only by browser automation (not by `mix test`, which doesn't have this race): the confirmation page's "Confirm" button relies on `phx-trigger-action`, a JS-driven form submit that only works once the LiveSocket has actually attached — clicking immediately after `page.goto` was a silent no-op in the verify script; fixed by waiting for network-idle first. Not a product bug (a human reading the page takes longer than that to find the button), but a real gotcha for any future Playwright verify against a magic-link confirm page.

</details>

<details>
<summary>Feature 15 — Cashier POS (2026-07-18)</summary> Built entirely on top of the customer's own cart/checkout/payment machinery rather than a parallel POS data model — the load-bearing decision of this feature: a cashier's ticket **is** a `Tabletap.Ordering.Cart` with a cashier-minted `guest_token` held in `Cashier.PosLive`'s socket state (never persisted to a URL/session — it only needs to survive one LiveView process), and "Charge" **is** `Ordering.checkout/2`, the identical one-way door the QR flow uses. This is the literal mechanism behind design-qa.md's "cashier as full customer proxy" (Q3) — every existing cart mutation (`add_to_cart/7`, `update_item/3`, `remove_item/2`, `set_kind/3`, `cart_total/2`) is reused unmodified; the only *new* cart-side surface was extending `Cart.kind`'s enum with `:counter` (walk-in tickets — `Order.kind` already had it since Feature 08). `orders.placed_by_membership_id` (deferred since Feature 08's own moduledoc) lands now, set automatically: `do_checkout/2` reads `scope.membership` — `nil` for a customer's own guest scope, set for any staff-driven checkout — no signature change to `checkout/2` itself.

**New settlement paths in `Tabletap.Payments`**, none of which call `accrue_platform_fee/2` (design-qa.md Q24 — the per-order fee only rides wallet charges, cash/comp are fee-free by design, not oversight): `record_cash_intent/2` (Q3 — the customer's own QR checkout choosing "Cash" parks a `pending` cash payment, order stays `pending_payment` under the same 12-min hold TTL a wallet charge gets), `verify_cash_payment/3` (the cashier's counter action — marks the parked payment succeeded + fires the order; an `expired` order gets **Revive** first — Q26 — re-reserving daily-limit holds the sweep released, `{:error, {:sold_out, item_name}}` via the new `Ordering.first_sold_out_item_name/2` if genuinely gone), `settle_cash_now/3` (the POS's own immediate tender — no pending phase, the cashier is standing right there), and `charge_comp/4` (Q30 — manager/owner only; internally an atomic whole-order 100%-discount via `Ordering.apply_discount/4` + a `provider: :comp` settle in one `Ecto.Multi`, so "comp" is one button, not "apply exactly 100% discount then pay"). `venues.pay_at_counter_enabled` (architecture.md's documented field name, not invented) is the Q3 toggle, off by default, added to `Manager.PaymentSettingsLive` independent of wallet `charges_enabled` — a venue can take counter cash with zero WaafiPay credentials on file.

**Discounts** (`Tabletap.Ordering.OrderDiscount`, architecture.md's exact documented schema): `apply_discount/4`/`remove_discount/2`, pre-payment only (Q36 — `{:error, :not_pending_payment}` once the order's moved on), whole-order or line-level, always attributed to a `staff_membership_id`. Per architecture.md's own wording ("Manager/cashier applied") a plain cashier can apply an ordinary discount with no role gate in the context itself — only the 100%-discount comp path is manager-gated, and that gate lives in `Payments.charge_comp/4`, not here.

**End-of-day close — the Z-report** (two new tables, `z_reports` + `z_report_cash_counts`, home'd in `Payments` rather than a new context since a future Feature 18 Report Center will likely absorb/rebuild it anyway): `z_report_preview/2` is a pure, repeatable read — order count, revenue/discount/refund totals, a per-payment-provider breakdown, and one expected-cash line per cashier (Q22: cash taken minus cash refunds **netted back against whichever cashier's payment they refund** — the drawer that gave the cash is the drawer it comes back out of, a deliberate simplification of "who reconciles a refund" rather than tracking a separate refund-time attribution). Every money event windows by the business day's **real cutoff-to-cutoff instants** (`DateTime.new!/3` against `venue.timezone`/`business_day_cutoff`) and counts on the day it actually happened, never the underlying order's `business_date` (Q37 — a late refund reports on its own day). `close_z_report/3` persists `z_report_preview/2`'s numbers as a point-in-time jsonb snapshot (Q38 "the original close stays visible as closed") — **stored as raw decimal + currency strings, deliberately never `Money.to_string!/2`**, which needs a locale and would hit the exact `"so"`-locale CLDR gap already flagged in this file's Feature 04 entry; reconstructed into real `Money` structs only at display time in `ZReportLive`. One row per venue per business date (`unique_index`); a second close attempt fails the constraint (error lands on `:venue_id`, the first field in the composite list — Ecto's convention, not `:business_date` as the first draft of this docstring assumed until a test caught it).

**`Cashier.PosLive`** at `/pos` (ui-rules.md "Surface: Cashier POS" — "speed of entry is the metric"): category rail + search + photo grid (reuses `Catalog.list_public_menu/1` unmodified — a cashier shouldn't ring up anything a customer couldn't order either), a modifier quick-sheet mirroring the customer sheet's selection logic, a running ticket panel with line qty +/−/void, a kind toggle (Walk-in/Dine in/Takeaway) + table picker (Charge is disabled for dine-in with no table chosen), and a payment screen (Cash with a change calculator, Wallet reusing `Payments.charge_order/3` verbatim and showing the same "waiting for PIN" state via the `order:<id>` PubSub topic the customer tracker itself subscribes to, Comp gated to `role in [:manager, :owner]` in the template), a Q3/Q26 verify+Revive tab (order-number lookup via the new `Ordering.get_order_by_number/2`, scoped to *today's* business date since numbers reset daily), and a shift-summary drawer (`Payments.cashier_summary/3`). Four screens, one `@overlay` assign, never separate routes — reloading between "ring up" and "take payment" would cost the one thing this surface is measured on. New `ScopeHooks.require_cashier_staff` (`[:cashier, :manager, :owner]`, same reasoning as Feature 14's `require_kitchen_staff` — a register is often minded by a manager filling in) gates both `/pos` and `/pos/z-report`. `signed_in_path/1` gained real clauses for both `:cashier` (`/pos`) and — closing a gap Feature 14 had flagged and left open — `:waiter` (`/waiter`).

**Two real bugs caught only by testing, both fixed:** (1) the payment screen's cash-tender `<form>` had its own `phx-submit="pay_cash"` *and* a separate "Confirm cash payment" button wired to the same event — pressing Enter in the tendered-amount field would silently charge the customer without the deliberate tap the button implies; fixed by routing the form's submit to the harmless `tender_change` no-op instead, so only the explicit button charges. (2) `Tenants.list_memberships/2` (a new display-lookup for the Z-report's per-cashier rows) used `preload: :user` through the tenant-enforced `Repo` — `users` has no `org_id` column, but `Repo.prepare_query/3` injects `org_id` into *every* query it runs, preload sub-queries included, so the very first real page load crashed; fixed with two explicit queries (memberships tenant-scoped as normal, `users` with `skip_org_id: true`, zipped together) — the established `Accounts`/`Tenants` pattern for touching `users` directly, just not one this feature's author had needed before.

**606 → new tests across 4 files = 606/606 green** (`test/tabletap/payments/pos_test.exs`: cash intent/verify/Revive/sold-out-naming, immediate tender, comp's manager gate + atomicity, Z-report totals/close/double-close/variance, cashier summary; `test/tabletap/ordering/discounts_test.exs`: discount stacking/removal/pre-payment guard, order-number lookup, `:counter` kind, `placed_by_membership_id` attribution; `test/tabletap_web/live/cashier/pos_live_test.exs` + `z_report_live_test.exs`: full ring-up-to-payment flows through the real stream/overlay path, access gating, the dine-in-needs-a-table guard, discount/comp UI, verify+Revive, closed-report persistence), `credo --strict` clean (one alias-order and one function-complexity finding fixed — `z_report_preview/2` split into four named per-query helpers), `format` clean. Verified against a real dev server via Playwright: a walk-in Flat White rang up and fired **in 692ms** (build-plan's verify step asks for "under 15 seconds"), a customer's pay-at-counter QR order showed its order number on the tracker exactly per Q3's spec and was verified+fired from the POS, an order was comped, and a Z-report closed with the drawer numbers reconciling to the cent (2 cash sales × $4.00 = $8.00 expected, a deliberately-entered $9.00 counted showing a +$1.00 variance in warning color) and persisting read-only across a reload. Zero console errors.

**`/review` pass caught 4 real issues before this shipped — all fixed, not deferred:**
1. **[Critical] `:counter` walk-in orders were never actually skipped from waiter assignment** — `Ordering.assign_waiter/3` had no kind check at all; a walk-in coffee at a `:waiter`-mode venue fell through to `assign_by_load/3` and got a real waiter assigned to an order with no table and no delivery step. Fixed with an `order.kind == :counter -> {:ok, :counter_no_assignment}` clause alongside the existing pickup-mode one; regression test added to `assignment_test.exs`.
2. **[Critical] "Cash refunds from the POS" was an explicit build-plan bullet with zero UI built** — `Payments.refund/5` already existed (Feature 09) but nothing in `Cashier.PosLive` called it. Added a `:refund` overlay: order-number lookup (`Ordering.get_any_order_by_number/2`, new — unlike the Q3 lookup, matches *any* status since a refund targets an already-settled order), cash-only (a non-cash payment shows "ask a manager" rather than mishandling a wallet refund with no round-trip built), amount pre-filled to the payment total, reason required.
3. **[Important] Violated ui-rules.md's own POS rule** ("items without [modifiers] go straight to the ticket") — every item tap opened the quick-sheet, even for a plain no-modifier coffee. Fixed: `open_item` now adds directly when `groups == []`; the modifier-sheet path and the fast path share one `do_add_to_ticket/4` helper so they can't drift on what "adding" means. New test confirms an item *with* modifiers still opens the sheet.
4. **[Important] `ZReportLive`'s close handler hard-matched `Decimal.parse/1` on raw cashier input** — any non-numeric "counted cash" entry crashed the LiveView (`MatchError`) instead of showing a validation message. Fixed with a `reduce_while` that bails to a flash on the first unparseable value; tightened to require the whole string parse cleanly (`{decimal, ""}`, not just a leading numeric prefix) while at it.

**612/612 green** after the fixes (one pre-existing concurrency test in `checkout_test.exs` — 10 concurrent checkouts hammering a limit of 3 — flakes under full-suite parallel load from DB sandbox pool contention, unrelated to this feature; passes cleanly every time in isolation).

</details>

<details>
<summary>Feature 14 — Kitchen Display System (2026-07-17)</summary>

Kitchen Display System. `TabletapWeb.Kitchen.BoardLive` at `/kitchen`: three status columns (New = `placed`+`accepted` | Preparing | Ready) on a new dark-only `Layouts.kds/1` shell (a `data-theme="dark"` wrapper pins daisyUI's dark tokens for the subtree — same forced-look reasoning as the marketing `.mk` scope; ui-tokens.md "KDS: always dark"). Route gating: a new `ScopeHooks.require_kitchen_staff` admits `[:kitchen, :manager, :owner]` — deliberately wider than `/waiter`'s strict gate, because the KDS is a *shared venue screen*, not a personal queue, and Q25 explicitly names both kitchen and manager as undo actors. `signed_in_path/1` now lands kitchen-role logins on `/kitchen` (**waiter parity gap flagged below** — waiter logins still land on `/`). Manager sidebar's "Kitchen · Soon" placeholder became a real link (+ mobile nav row).

**The kitchen context API** (`Ordering`): `list_kitchen_orders/1`/`get_kitchen_order/2` (the `@handoff_statuses` set, oldest placed first, `menu_item` preloaded for prep thresholds — never for live prices; tickets render snapshots only), `kitchen_start_order/2` — **a `placed` ticket passes through `accepted` on its way to `preparing`** (the machine has no `placed → preparing` edge, and starting to cook *is* acceptance; also the only accept path at a pickup-mode venue, where no waiter exists — the waiter's own Accept button only shows on `placed`, so it simply disappears), `kitchen_mark_ready/2`, `kitchen_undo/2` (Q25's two one-step-backs; everything else `{:error, :stale}` — a racing tablet's tap flashes "moved on" + full board reload, never a crash), and `expected_prep_minutes/1` (slowest line's `prep_minutes`, 10-min default; **no queue-depth or Busy-Mode inflation** — those manage the customer's expectation, the cook's overdue clock starts honest; `estimated_minutes/2` now delegates to it for its base).

**Stream-targeted realtime** (ui-registry.md's "never re-query the whole list on an event", honored for the first time because this page runs a 12h shift): the `venue:<id>:orders` broadcast payload changed from bare `:order_updated` to **`{:order_updated, order_id}`** (state machine + `flag_order/3` + `broadcast_order_updated/2`; `Manager.OrdersLive`'s `handle_info` clause updated — the `order:<id>` tracker topic keeps its old shape). The board keeps a `column_index` map (order id → column) and, per event, re-fetches just that order and `stream_insert`/`stream_delete`s the one ticket — same-column updates patch in place, arrivals push a `"kds:new-ticket"` event for the sound cue; the full re-read happens only on mount/reconnect and stale taps. **Waiter notified on Ready** (the build-plan bullet): the state machine now broadcasts `{:order_ready, id}` on the assigned waiter's topic at `ready` — and `{:order_ready_retracted, id}` on the Q25 undo ("retracts the waiter's pickup notification"); `Waiter.QueueLive` flashes + reloads on both.

**Ticket UI** per ui-tokens.md's KDS Ticket spec: 6px status left border, 22px/800 tabular timer in the status color, ≥16px body, modifiers + notes rendered in full always (Q12 — a nut-allergy note is the test's literal fixture), `:contains_86d_item` warning badge (Q27's KDS half; the manager-alert half shipped in Feature 13), 56px full-width footer advance (Start → Ready; a `ready` ticket's footer is a passive "Waiting for pickup" — serving is the waiter's scan, Feature 11), small header undo on `preparing`/`ready`, 250ms entrance animation. **Overdue is fully client-side**: the colocated `.TicketTimer` hook ticks elapsed from `data-started-at` (server sends `placed_at` once — no per-second server messages) and toggles a `kds-overdue` class past `data-expected-minutes` — CSS flips the header to warning/15 and pulses the timer (reduced-motion: static color, no pulse). Two more colocated hooks: `.KdsSound` (WebAudio two-tone beep on new tickets, per-device localStorage toggle — no audio asset to fetch) and `.KdsReload` (12h-uptime refresh prompt, the "nightly reload" mitigation from Known Technical Risks). Registry divergences, same shape as Feature 10's: `kds_ticket` lives as a private component in `BoardLive` (not the registry's planned shared `order_components.ex`), and `TicketTimer` is colocated rather than an `assets/js/hooks/` file (Feature 11's convention: external files only when an import trick demands it).

**Gotcha for future streams work:** an empty-state element inside a `phx-update="stream"` container **must have its own DOM id** — LiveView raises "setting phx-update to stream requires setting an ID on each child" otherwise; AGENTS.md's `hidden only:block` example omits the id and won't survive contact.

**545 → 24 new tests = 569/569 green** (`kitchen_board_test.exs` context: start-chains-through-accepted, undo clears `ready_at`, stale races, second-tenant isolation, ready-notification + retraction broadcasts; `board_live_test.exs`: column placement, full modifier/note render, Start/Ready/undo through the real stream path, 86 badge, stale-tap flash, live arrive/leave, kitchen-role access + waiter denial), `credo --strict` clean, `format` clean. **gettext caught up**: `mix gettext.extract --merge` hadn't been run since Feature 03 — 339 accumulated msgids from Features 04–13 landed in `default.po` with this feature's strings. Verified against the real dev server via Playwright, kitchen tablet at 1024×768 + customer tracker at 390px in a separate context: board renders dark with correct columns/counts, a zero-prep espresso ticket pulses overdue immediately, Start moves the ticket (tracker follows to Preparing live, no refresh), Ready then undo brings it back (tracker retracts too), and **a second kitchen tab's tap moved the ticket on the untouched first tab** — zero console errors. (Verify-script caveat, not a product bug: the seeder runs on a separate BEAM node, so its PubSub can't reach the server — in-server *arrival* of a brand-new order is covered by the LiveView test; real checkouts run inside the server.)

<details>
<summary>Features 12–13 — Ingredients & Recipes, Stock Ops & Alerts (2026-07-14)</summary> `Tabletap.Inventory` (schema-only since Feature 11) gets its real CRUD/UI: `Ingredient` creation deliberately **never accepts `stock_qty` as input** — a fresh ingredient always starts at zero and the manager restocks it from there (`Inventory.restock/5`), so every unit of stock that ever existed has a ledger row explaining it, no "opening balance" exception to the append-only-ledger rule. `Inventory.UnitInput.parse/2` is the one place unit conversion happens ("1.5kg" → `1500` for a `:g` ingredient) — every quantity field across the ingredient form, recipe editor, and restock/adjust/wastage actions routes through it once, at the input boundary, never re-converted downstream.

**Recipe editor** lands on `Manager.MenuLive`'s item modal as a new "Recipe" section, structurally identical to the existing "Modifier groups" attach/detach pattern (`Inventory.add_recipe_line/4`, `list_recipe_lines/2`, `remove_recipe_line/2`). **Modifier ingredient deltas** (`modifier_options.ingredient_id`/`ingredient_qty_delta`, architecture.md's data model, deferred since Feature 05's own moduledoc) land now on `ModifiersLive`'s option form as optional fields — both-or-neither validated (a delta with no ingredient chosen, or vice versa, is a half-filled form, not "no stock effect"). `Inventory.deduct_for_order/2` (Feature 11) now folds modifier deltas in on top of the base recipe per line — "extra cheese" adds, "no onions" subtracts, both `× qty` — and a line that nets to exactly zero for an ingredient (a removal offsetting its own recipe amount) writes no movement at all rather than a zero-quantity audit row.

**Stock operations**: `Inventory.restock/5` (records `unit_cost` paid, refreshes `ingredients.cost_per_unit` to it), `adjust_stock/4` (+/-, always reasoned, negative resulting stock allowed — design-qa.md Q14 "service never blocks on bookkeeping"), `log_wastage/4` (always negative, reason required) — all `stock_movements` rows through one shared `StockMovement.movement_changeset/1` (`:restock`/`:wastage`/`:adjustment`; the existing narrow `deduction_changeset/1` stays `:sale`-only, Feature 11's own path, untouched). **Low-stock detection runs after every stock-decreasing write** (sale, wastage, negative adjustment — never restock or a positive adjustment, which can only make things *more* fulfillable) and broadcasts on `venue:<id>:inventory`; the new `Manager.IngredientsLive` at `/inventory` subscribes for a live low-stock/negative-stock banner with zero refresh needed — the build-plan verify step's "dropping cheese below threshold pings the manager dashboard live."

**Restock report** (`Inventory.restock_report/1`: every thresholded ingredient at or below it, `suggested = threshold × 2 − current`, most urgent first) powers three surfaces that all read the exact same function so they never disagree: `Manager.RestockReportLive`, a hand-rolled CSV controller (`Manager.RestockCsvController` — five columns of already-safe data needs no dependency, code-standards.md's "does Elixir already do this?" check), and `Manager.RestockPrintLive` (mirrors `TablePrintLive`'s print-CSS/hook pattern exactly, laid out as a purchase-order sheet instead of a QR grid).

**Auto-86 (design-qa.md Q11) + Q27 open-ticket flagging**: after any stock-decreasing write, `Inventory` checks every `available_today` menu item referencing the moved ingredient — if its *base* recipe (modifier deltas deliberately excluded; that's a per-order question the kitchen already sees at serve time, not a menu-wide availability one) can no longer be fulfilled from current stock, it calls back into `Catalog.set_availability/3` to 86 it and `Ordering.flag_open_orders_containing_item/2` to flag every still-in-the-kitchen order (`placed`/`accepted`/`preparing` — a `ready` order is already made, 86'ing now changes nothing for it) with a **new third `Order` flag value, `:contains_86d_item`** (joining `:unserveable`/`:not_picked_up` on the same shared column — the three never collide since Q27 only ever touches pre-`ready` orders while the other two only ever touch `ready` ones). Surfaces on the existing `Manager.OrdersLive` "Needs your attention" board for free — a new badge, a "Kitchen confirmed — still makeable" action (`Ordering.mark_still_makeable/2`, clears the flag, no status change) alongside the Refund button every flag type already gets (`refund_reason/1` gained a clause; `resolve_flag_refund/3` needed no other changes — `:refunded` was already legal from `placed`/`accepted`/`preparing`). Manual 86 needed no new code at all — `Catalog.set_availability/3` (Feature 04) already was the kill-switch, `available_today` already was the mechanism.

**Stocktake** (design-qa.md Q14/Q43): new `stocktake_sessions`/`stocktake_lines` tables. `Inventory.start_stocktake/1` snapshots every non-archived ingredient's current `stock_qty`/`cost_per_unit` into its own line the moment the session opens — Q43's "session snapshots theoretical quantities at start," so sales during the count can't move the number the count gets compared against (only one session open per venue; starting a second is `{:error, :already_open}`). Two genuinely different numbers come out of `close_stocktake/2`, both deliberate: the **report's variance** is `counted − theoretical_snapshot` (the honest, sales-uncorrected diagnostic number Q43 defines — "recommend counting at close" exists precisely because a long-open session inflates this via legitimate interim sales), while the **reconciling `:adjustment` movement** is `counted − current_actual_stock_at_close` (brings the ledger to match physical reality regardless of how much sold during the count — confirmed with a dedicated test: sell mid-session, count something else entirely, assert the report shows the "wrong" sales-uncorrected number while the ledger still ends up exactly at what was counted). `Manager.StocktakeLive` warns (non-blocking, `Ordering.count_open_orders/1`) when orders are still open but never prevents starting.

**503 → 39 new tests** (`test/tabletap/inventory_test.exs` + `inventory/unit_input_test.exs`) **= 542/542 green**, `format --check-formatted` clean, `credo --strict` still only the same 5 pre-existing Feature-10 findings (untouched, not mine — same ones flagged and left alone in the Feature 11 entry below). No plan gating (role-features.md marks ingredients/stock-ops/alerts/stocktake **Growth+**) — deliberately deferred, build-plan.md Feature 19 explicitly owns "Inventory ... nav/routes check `org.plan` via a `Plans` context helper," which doesn't exist yet; this context and its UI are fully built and open to every org until that gate lands.

</details>

<details>
<summary>Feature 11 — Serve Confirmation & Stock Deduction (2026-07-14)</summary>

New **`Tabletap.Inventory`** context (`ingredients`/`recipe_lines`/`stock_movements`, composite `(id, org_id)` FK pattern), landing early and schema-only ahead of Feature 12's actual CRUD/recipe-editor UI — same deferral pattern `Tenants.Venue` and `Catalog`'s Feature 05 moduledoc already used for not-yet-needed columns. `Inventory.deduct_for_order/2` is wired into `OrderStateMachine.transition/3` as an `Ecto.Multi.run` step on every `_ -> :served` transition (atomic with the status write — a deduction failure rolls the `served` write back with it); an item with no seeded `recipe_lines` deducts nothing, a correct no-op until Feature 12 ships real recipes, not an error.

**Serve confirmation** (`Ordering.confirm_served_by_scan/3`, design-qa.md Q18): the scanned value must equal `Ordering.serve_token/2` — the order's table `qr_token` normally, or the customer's own `guest_token` for a table-less (takeaway) order *or any order at a pickup-mode venue, even one that has a table_id* (pickup venues never scan a table, per Q18 — this venue-level branch was added after the first pass only checked `table_id`, caught while wiring the order tracker's own "show this QR" gate off the identical function to avoid the two ever drifting apart). Vendored `qr-scanner` (code-standards.md's pre-approved JS dep) — the **library's own worker-fallback script (used when the browser has no native `BarcodeDetector`, e.g. Safari) does its own internal relative `import()`, which resolves against wherever the *importing module* was actually loaded from**: bundling `qr-scanner.min.js` into `app.js` (a classic, non-module script) would have made that resolve against the *page's own URL* instead, 404s that vary by route. Fixed by loading it as a real separate ES module via a **runtime** `import(someVariable)` (esbuild only statically bundles a dynamic import when the argument is a literal string, so a `const` reference is deliberately left as a true runtime import) from a fixed `/vendor/qr-scanner.min.js` static URL — both that file and its worker sibling live in `priv/static/vendor/` (tracked in git like fonts/images, not esbuild output), added to `TabletapWeb.static_paths/0`. New `assets/js/hooks/qr_scanner.js` (ui-registry.md's planned `QrScanner` hook), wired into `app.js`'s `hooks:` map alongside the colocated ones (this is the project's first hook to live at `assets/js/hooks/`, matching ui-registry.md's stated convention rather than a colocated `:type={Phoenix.LiveView.ColocatedHook}` block, since it needs the runtime-import trick above).

**Manager-only manual serve confirm** (`Ordering.confirm_served_manually/2`, Q19) and **flag resolution** (`resolve_flag_refund/3`, `convert_to_takeaway/2`, `mark_collected/2`, `close_as_wasted/2`, Q9/Q10/Q32) land on a new `TabletapWeb.Manager.OrdersLive` at `/orders` — the nav slot for "Orders" already existed as a `.manager_nav_soon` placeholder (added ahead of time when the sidebar was built), now a real link. No cashier POS exists yet (Feature 15), so this page is the only place these resolutions happen for now; role-features.md's "manager/POS resolves" is just "manager" until then. `resolve_flag_refund/3` calls `Payments.refund/5` for a real payment or settles a `provider: :comp` order with no provider round-trip (Q30); `close_as_wasted/2` still deducts stock (`served` first, then `closed` — food was made even though nobody collected it, same comp-vs-void discipline Q30 established).

**Pickup no-show sweep** (`Ordering.Workers.SweepPickupNoShows`, Q32, every 5 min) flags a pickup-mode venue's `ready` order `:not_picked_up` once it's sat past the venue's new `pickup_timeout_minutes` (default 15, `Tenants.Venue`) — cross-tenant loop shape matches `SweepAbandonedCarts`/`SweepExpiredHolds`, but flags per-order through `Ordering.mark_not_picked_up/2` (broadcast-per-order) rather than a bulk `update_all`. **`Ordering.Workers.AutoCloseServedOrders`** (hourly) closes `served` orders past the 24h rating window — same cross-tenant loop, but status changes still only ever go through `OrderStateMachine.transition/3` (code-standards.md), never a bulk `update_all` on `status` the way the flag sweep can get away with.

**Customer tracker** (`Public.OrderTrackerLive`) gained a celebratory "Order served — enjoy!" panel at `:served`, and — for exactly the orders `Ordering.serve_token/2` would check against `guest_token` — a "Show this to staff" QR block (same `qr_code` dep/pattern as the table print sheet, `:high` error correction, raw token encoded since it's scanned, never navigated to).

**328 → 21 new tests** (`test/tabletap/ordering/serve_confirmation_test.exs`) **= 503/503 green**, `format --check-formatted` clean. `credo --strict` has 5 pre-existing findings (alias-order/nesting nits in `ordering.ex:687`, `order_state_machine.ex:122`, `tenants_fixtures.ex:91`, `assignment_test.exs:18`, `staffing/workers/auto_close_shifts.ex:17`) — all from Feature 10's own uncommitted work, none touched or introduced by Feature 11, left as-is rather than scope-creeping into someone else's in-flight commit. Full loop verified against a real dev server via Playwright at 390px, fake-camera-device Chromium flags for a real `getUserMedia` round-trip: manager `/orders` → Manual serve confirm flips a `ready` order to `served` and it drops off the Ready list; a flagged order's Convert-to-takeaway resolves it off the Needs-attention list; waiter queue's Scan-to-serve opens a live camera feed with the scan-region overlay rendering correctly (also separately verified the *no-camera* error path: a clean "Couldn't access the camera: Camera not found" flash, no JS crash); customer tracker shows both the served celebration and the takeaway serve-QR. Zero browser console errors in either staff surface.

**Also fixed in passing**: `Tabletap.Tenants.Venue`'s `pickup_timeout_minutes` migration column had no matching schema field (would have silently 500'd the first time anything read it) — added.

</details>

<details>
<summary>Features 08–10 (2026-07-13, built in a prior session — no detailed build narrative was captured here at the time; summarized retrospectively from the shipped code)</summary>

**Feature 08 — Orders & State Machine**: `orders`/`order_items`/`order_item_modifiers` (snapshot fields), `Ordering.OrderStateMachine` (full transition table incl. one-step-back undo, Q25), atomic daily-limit hold-at-checkout + `SweepExpiredHolds` 12-min TTL sweep, Busy Mode (`Venue.pause_changeset`/`eta_inflation_factor`), `Public.OrderTrackerLive` at `/orders/:guest_token`.

**Feature 09 — Wallet Payments**: `Tabletap.Payments` context, `Payments.Provider` behaviour + WaafiPay adapter, encrypted merchant credentials (`Tabletap.Encrypted`), push-PIN charge flow, webhook + `ReconcilePendingPayments` poller, refunds (`Payments.refund/5`), `platform_fee_ledger`.

**Feature 10 — Waiter Assignment & Waiter App**: `shifts`/`waiter_calls`, `TabletapWeb.Presence` (30s flap grace), `Ordering.assign_waiter/3` (same-table stickiness → lowest-load → solo-waiter auto-accept → 90s escalation → claim board), `Waiter.QueueLive` at `/waiter`, `Ordering.mark_unserveable/2` (Q9, flagging only — resolution landed in Feature 11 above).

</details>

---

<details>
<summary>Feature 07 — Customer Menu & Cart (2026-07-12)</summary>

New **`Tabletap.Ordering`** context (matches architecture.md's module tree exactly — `ordering/` was always meant to own carts/orders/state-machine), starting with carts only; `orders` lands in Feature 08. Three tables on the same composite `(id, org_id)` FK pattern: `carts` (venue-scoped; `table_id` nullable for takeaway; `guest_token`; `kind` enum `dine_in`\|`takeaway`; `status` enum `active`\|`converted`\|`abandoned`; partial unique index on `(guest_token, venue_id) WHERE status = 'active'` — one live cart per guest per venue), `cart_items` (`menu_item_id` FK is `:restrict` not `:delete_all` — same archive-not-delete reasoning as `menu_items.category_id`; each "add to cart" creates a new line rather than merging into an identical existing one, a deliberate simplicity call not specified in the docs), `cart_item_options` (pure join to `modifier_options`, mirrors `item_modifier_groups`). **Carts are always live-computed, never snapshotted** — `Ordering.cart_total/2`/`line_total/1` read the menu's *current* price and option deltas on every call; prices only freeze once a cart converts into an order (Feature 08). `customer_user_id` from architecture.md's data-model row is deliberately not a column yet, same deferral pattern `Tenants.Venue` used for its own Feature-08/09-only fields — nothing references customer accounts until Feature 16.

**Structural revalidation (design-qa.md Q42)** lives in `Ordering.validate_line/2` + the public `unsatisfied_groups/2` helper: re-checked live on every cart-view render (no stored "invalid" flag to go stale), catching a group's min/max/required changing — or a group being detached entirely — after a line was added; an invalid line shows "please remove and re-add it" instead of silently mispricing or crashing. The exact same check runs at `add_to_cart/7` time too, so a bad selection never reaches the database in the first place.

**Guest identity required solving a real LiveView constraint, not just a design decision**: `phoenix_live_view` has no API to write an HTTP cookie from inside a connected socket (confirmed by reading the library source — no `put_session`/cookie-setting function exists on `Socket`, since there's no HTTP response to attach `Set-Cookie` to mid-connection). So `TabletapWeb.GuestToken` (a plug, mirroring `UserAuth`'s shape) only *restores* an existing cookie into the session before mount; minting a fresh `guest_token` happens inside the LiveView's "add to cart" handler (architecture.md: "minted on first add," not on every page view) and is written to the browser via `push_event/3` + a colocated `.GuestToken` hook doing `document.cookie` directly (not `HttpOnly` — an accepted tradeoff for a low-sensitivity anonymous cart identifier, same class of decision as the existing `phx:set-theme` client-storage hook). Router gained a new `:guest_token` pipeline (`plug :fetch_cookies` added to `:browser`, scoped restoration plug on the public customer scope only). The full Q13 "30-day cookie survives closing the browser + active-order banner on re-scan" behavior is Feature 08's order-tracker bullet, not this one — Feature 07 only needed same-tab reconnect/deploy survival (Q50), which the session alone already provides once a token exists.

**Cross-tenant background job pattern established for the first time** (`Ordering.Workers.SweepAbandonedCarts`, hourly cron, marks 24h-stale active carts `abandoned` — rows kept, not deleted): reusing `Tabletap.ObanRepo` was considered and rejected — its moduledoc scopes it explicitly to Oban's own bookkeeping tables, and using it for tenant-owned `carts` data would functionally bypass the tenant guard from a context (`Ordering`) not on the `skip_org_id: true` exception list, even without literally passing that option. Instead added `Tenants.list_org_ids/0` (`skip_org_id: true` — allowed, `Tenants` is on the list) as the sanctioned enumeration entry point, and the worker loops it calling `Repo.put_org_id/1` once per org before a normal tenant-scoped `update_all` — `prepare_query/3` injects that org's `where: org_id == ^id` automatically (repo.ex), so every write goes through the exact same enforcement path a request-scoped query would. Establishes the pattern for every future cross-tenant sweep (expired-hold release in Feature 08, etc.).

**UI**: `Public.MenuLive` rewritten as one LiveView with three overlay states (`:none`, `{:item, item, groups}`, `:cart`) rather than separate routes — matches how `Manager.MenuLive` already layers its item-edit modal, and both the item detail sheet and cart are genuinely "bottom sheets" per ui-tokens.md, not distinct screens. Category tabs (anchor-scroll, shown only when >1 category), sold-out state from `Catalog.list_daily_limits`/`DailyItemLimit.remaining/1` (photo grayscale + badge + `phx-click` omitted entirely — HEEx drops an attribute whose value is `false`, so a sold-out card is structurally not tappable, not just visually disabled). Item detail sheet: radio semantics for `max_selections == 1` groups (selecting one clears any other in that group), checkbox semantics with a hard cap otherwise (a click past `max_selections` is silently ignored, not an error); default options (`ModifierOption.default`) pre-select on open with zero customer interaction; a group's requirement chip only turns `badge-error` after a failed submit attempt, matching ui-tokens.md's Modifier Sheet spec precisely. **Deliberately no "place order"/payment button anywhere** — ui-tokens.md's own Sticky Cart Bar spec labels its action "View cart," not "Checkout," and no build-plan Feature-07 bullet mentions a submit action; Orders (Feature 08) is what turns a cart into something payable, so shipping a stub button here would be exactly the "half-finished implementation" code-standards.md warns against.

**242 → 313 tests green**, `credo --strict`/`format --check-formatted` clean (two credo findings fixed post-write: alias-group ordering, and a 3-deep nested `case` in `find_or_create_cart` flattened into two named helpers). Full build-plan verify scenario run against a real dev server via Playwright at 390px — signup → table → "Burger" $5.00 → modifier groups "Extras" (Extra cheese +$1) and "Remove" (No onions $0), both attached → **two separate browser contexts scanning the same table's QR**: guest A customizes (extra cheese, no onions) → live "Add · $6.00" button → cart shows both modifiers and the correct total; guest B (fresh context, same table) sees no cart at all, orders a plain burger, gets $5.00 with zero leakage of guest A's selections in either direction → guest A's browser **reloaded** (simulating a reconnect/deploy) and the cart with its modifiers was still there. Matches the build-plan verify step exactly ("cart shows the right total. Two carts on two phones at the same table stay independent").

</details>

---

<details>
<summary>Feature 06 — Tables & QR Codes (2026-07-12)</summary>

New `tables` table, but living in **`Tabletap.Tenants`**, not `Catalog` — its public `/t/:qr_token` lookup needs `skip_org_id: true`, which code-standards.md restricts to `Accounts`/`Tenants`/platform-admin, and a table is a venue child-entity exactly like `memberships` (already in Tenants). Same composite `(id, org_id)` FK pattern as venues/menu_categories/modifier_groups; `qr_token` is a `unique_index` on its own (not scoped to venue — it's the whole point of an opaque token), `number` is a string (so "A3"/"Patio 1" work, not just integers) with a partial unique index on `(venue_id, number) WHERE archived_at IS NULL` so an archived table's old number is instantly reusable. Archive-not-delete (Q41), same shape as categories/items. `Table.generate_qr_token/0` is `:crypto.strong_rand_bytes(16) |> Base.url_encode64(padding: false)` — assigned on create and reassigned by a separate `rotate_changeset/1` (Q7: rotating kills the old printed code; a plain edit never touches the token). `Tenants.get_table_by_qr_token/1` mirrors `get_venue_by_slug/1`'s pre-auth `skip_org_id: true` shape exactly, filtering `active == true and is_nil(archived_at)` — an unknown, rotated, archived, or deactivated token all return `nil` alike, no distinguishing signal leaked. Public entry point: `GET /t/:qr_token` is a **plain controller** (`Public.TableController`), not a LiveView — the resolved `table_id` has to persist in the HTTP session (Q50, survives reconnects/deploys) and a LiveView process can't write session cookies itself; on success it redirects into the existing `/venues/:slug/menu` LiveView (`Public.MenuLive`), which now reads `session["table_id"]` and shows "Table 12" under the venue name when present. **A real bug found and fixed only by driving the actual not-found path in a browser**: the original implementation followed `Public.MenuLive`'s own pre-existing "venue not found" pattern (`put_flash` + `redirect(to: ~p"/")`) — but the root layout deliberately has no sitewide `<.flash_group>` (it would double-render on every LiveView page, which mounts its own via `Layouts.app`/`Layouts.manager`), so that flash was silently never shown; a guest scanning a torn/stale QR landed wordlessly on the marketing homepage. Fixed by rendering a small dedicated `TabletapWeb.Public.TableHTML` "This QR code isn't active" page directly (HTTP 404, no flash/session dependency) instead of redirecting — the same latent bug still exists in `Public.MenuLive`'s unknown-venue-slug path (pre-existing, not introduced here, not fixed — flagged below, not blocking). Manager UI: new `TabletapWeb.Manager.TablesLive` at `/tables` (CRUD + rotate + archive, same list/form pattern as `ModifiersLive`) and `TabletapWeb.Manager.TablePrintLive` at `/tables/print` (SVG QR grid via the already-approved `qr_code` dep, `:high` error correction for laminated/dirty codes, `:high`-appropriate `structure`/`scale`; each card shows the venue name + the scan URL in plain text underneath, per Q7's anti-phishing cue; `print:hidden` screen chrome + a `.Print` colocated hook wired to `window.print()`). "Tables" nav entry activated (was a "Soon" placeholder) in both the desktop sidebar and mobile nav row.

**A `/code-review`-style pass after the build found two more real issues, both fixed**: the print sheet's grid didn't match ui-tokens.md's explicit "2×4 grid per A4 page" spec — confirmed empirically with real print-media emulation (Tailwind's `sm:` breakpoint still evaluates true under print, since it compiles without a `screen` media qualifier), fixed with an explicit `print:grid-cols-2` override; and a forged/stale table id passed to `rotate_token`/`archive_table`/`edit_table` crashed with `FunctionClauseError` instead of failing gracefully, fixed with a `not_found_flash`/`reset_form_not_found` pair (mirrors an identical pre-existing weakness in `ModifiersLive`, not introduced here, not fixed there).

266/266 tests green (was 242), `credo --strict`/`format --check-formatted` clean. Full build-plan verify scenario run against a real dev server via Playwright at 390px, using a **fresh (logged-out) browser context** for the guest-facing steps to prove no session leakage between manager and guest: signup → `/tables` empty state → create "12" (label "Window booth") → duplicate "12" rejected inline (`has already been taken`) → print sheet shows a crisp SVG code + plain-text scan URL → **scanning the QR link in a brand-new browser context** opens that venue's live menu showing "Table 12" → rotate token → the old link now 404s with the honest not-found page (new token still resolves) → archive removes the table from the board. Screenshots checked for phone-width layout regressions (none).

</details>

---

<details>
<summary>Feature 05 — Modifier Groups (2026-07-12)</summary>

Three new tables on the same composite `(id, org_id)` FK pattern: `modifier_groups` (venue-scoped; `min_selections`/`max_selections`/`required`), `modifier_options` (group-scoped; `Money.Ecto.Composite.Type` `price_delta` — zero and negative deltas legal; `default` pre-select flag, `active` temporary-hide toggle, `position`), and `item_modifier_groups` (pure join with per-item `position`, unique `(item_id, group_id)`; hard-deleted freely — order snapshots copy names/deltas, so attachments carry no history). Groups **and** options get `archived_at` per Q41's general rule even though architecture.md's data model lists only `active` on options — Feature 08's `order_item_modifiers.option_id` will FK-reference options, so they must never hard-delete; the `(id, org_id)` unique index on `modifier_options` is already in place as that FK's target. Architecture's `ingredient_id`/`ingredient_qty_delta` option columns are **deferred to Feature 12** (no `ingredients` table exists to reference yet — same deferral as menu-item recipes). Validations per the build-plan verify step: `max >= min` and required→`min >= 1` (a required group you can pick zero of is a contradiction), plus `min >= 0`/`max >= 1`. Semantics decision: `max_selections` counts *distinct* options, each pickable once (the "extra cheese ×3" reading would need per-option quantities — not built, revisit only if a real venue asks). `Catalog.archive_modifier_group/2` archives and detaches from all items in one `Ecto.Multi`. `Catalog.price_range/2` (the Feature-05 "price-delta preview") is a pure function over an item + its attached groups: forced picks = the `min_selections` cheapest/dearest active options per bound, then optional picks only while they still move that bound (negative deltas lower the minimum, positive raise the maximum), clamped to the option count. UI: new `TabletapWeb.Manager.ModifiersLive` at `/menu/modifiers` (group library — groups are reusable across items, so they get their own screen, not a per-item editor) + attach/detach select and live price-range line inside MenuLive's item-edit modal; "Modifiers" nav link added to the manager sidebar/mobile nav. Zero new dependencies.

242/242 tests green (was 208), `credo --strict`/`format --check-formatted` clean. Full build-plan verify scenario run against a real dev server via Playwright at 390px: signup → create "Cheese" (min 0 max 3, Extra cheese +$1.00 rendering as "+$1.00") and "Remove" (No onions/No pickles, $0) → both invalid configs (max<min, required-with-min-0) rejected inline with field errors → "Hamburger" $5.00 created → both groups attached from the item modal → "Price with options: $5.00 – $6.00" shown → detach works. Screenshots checked for phone-width layout regressions (none).

</details>

---

<details>
<summary>Feature 04 — Menu Builder (2026-07-11)</summary>

`Tabletap.Catalog` context: `menu_categories`/`menu_items`/`daily_item_limits` schemas + migrations (composite `(id, org_id)` FK pattern, same as venues/memberships), scope- and venue-scoped throughout (org_id alone isn't enough — an org can have >1 venue). `menu_items` carries two independent booleans — `active` (persistent on/off, survives archiving-adjacent edits) and `available_today` (daily-reset 86-style toggle) — plus `dietary_tags`/`allergen_tags` array columns and a `Money.Ecto.Composite.Type` price. Archive-not-delete (Q41) on categories/items via `archived_at`; hard delete not built (not needed yet). Drag-to-reorder via plain integer `position` + full resequence in one `Ecto.Multi` transaction, driven by a colocated JS hook doing native HTML5 drag-and-drop (no new JS dependency). Photo upload via a `Tabletap.Storage` behaviour — `Storage.S3` (Supabase Storage's S3-compatible API, reusing the already-pinned `ex_aws_s3` presigned-PUT pattern) and `Storage.Local` (dev fallback with no Supabase project configured, and always in test) — both hand out a same-shaped presigned-PUT URL so the LiveView upload flow (`allow_upload external:`) doesn't care which is active. `dotenvy` (dev/test only) loads `.env` for Supabase credentials; `.env.example` documents the keys.

**New shared primitives added because Feature 04 needed them, not previously built:** `Tenants.business_date/2` (business-day-cutoff-aware "today," referenced by CONTEXT.md/code-standards.md since Feature 03 but never implemented until a real caller — daily limits — needed it) required adding a real IANA timezone database (`tz`, not `tzdata` — the latter's `hackney ~> 1.17` requirement conflicts with `ex_aws`'s `hackney ~> 4.0`). `Tenants.get_venue_by_slug/1` (skip_org_id, public/pre-scope, mirrors `get_valid_staff_invite_by_token/1`'s shape) is the entry point for `TabletapWeb.Public.MenuLive` (`/venues/:slug/menu`, temporary — Feature 06 fronts this same LiveView with real `/t/:qr_token` resolution instead of building a second one) — the caller calls `Repo.put_org_id/1` itself afterward, exactly like `UserAuth` does for staff requests, so `Tabletap.Catalog` never needs `skip_org_id: true` anywhere (code-standards.md's zero-tolerance list stays just `Accounts`/`Tenants`/platform-admin).

**Bugs found only by testing/running for real, both fixed:**
1. `Catalog.move_item_to_category/3` checked only the *destination* category's `venue_id`, not the *item's own* — a cross-venue move slipped past the app-level guard and hit a raw unhandled `Ecto.ConstraintError` at the composite-FK layer instead of a clean `{:error, changeset}`. Fixed both: the app-level check now compares both, and `MenuItem.category_changeset/3` declares `foreign_key_constraint(:category_id)` as defense in depth.
2. `root.html.heex`'s utility bar assumed `@current_scope` truthy implies `@current_scope.user` exists — true for every staff page so far, but a guest/customer scope (`%Scope{role: :guest}`, `user: nil`) is truthy with no user, so the very first public LiveView crashed on `@current_scope.user.email`. Fixed at the root (`@current_scope && @current_scope.user`) since every future customer-facing page (Feature 07+) would hit this identically.

208/208 tests green (was 148), `credo --strict`/`format --check-formatted` clean. Full flow verified on a real running dev server via Playwright (phone-width viewport): signup → create category → create item with a real photo upload (`Storage.Local`, confirmed served back from `/uploads/...`) → public menu shows it → manager toggles `available_today` off → public menu (already open, no reload) hides the item instantly via the `"venue:<id>:menu"` PubSub broadcast. A real layout bug (item row overflowing/overlapping on a 390px-wide screen) was caught by this and fixed.

**Known gap, not blocking, flagged for a fast-follow:** `venue.locale` defaults to `"so"` (Somali), but the CLDR/`:localize` backend has no data for it — `Money.to_string!/2` silently falls back to locale `:und`, producing `"US$ 3.50"` instead of `"$3.50"` on the public menu. Pre-existing gap (not introduced by Feature 04), surfaced by this being the first real customer-facing price display. Needs a decision: add proper Somali CLDR data, or pick a different fallback locale for money formatting specifically.

</details>

---

<details>
<summary>Feature 03 — Tenancy Core (2026-07-08)</summary>

Tenancy Core. `orgs`/`venues`/`memberships`/`staff_invites` schemas + migrations, composite `(id, org_id)` FK on `venues` so a child row can never point at another tenant's venue (verified with a dedicated test — a cross-org `Membership` insert fails the FK constraint, not just app-level validation). `Tabletap.Repo` tenant enforcement (`put_org_id/1`, `prepare_query/3` raise, `default_options/1`) per library-docs.md's pinned pattern — applies to `all`/`one`/`get`/`update_all`/`delete_all`, not to plain `insert`/`update` (those aren't query-based; insert-side correctness rests on the composite FK instead, documented in `repo.ex`'s moduledoc). `Tabletap.Tenants` context: `create_org_with_owner/1` (one transaction: user + org + venue + owner membership), `build_scope/2` (resolves org/venue/role from a user's memberships, session-aware for the venue switcher), `list_venues/1`, `switch_venue/2`. `/users/register` repurposed as the org-signup flow (business name, city picker → resolves currency/timezone, email, password — Q47 requires one for owners) — the only thing that route is ever used for in this app; submitting logs the owner straight in via the existing `phx-trigger-action` idiom and lands on a new minimal `/dashboard` (`TabletapWeb.Manager.DashboardLive`) showing venue/org/role/trial countdown + a venue switcher when an org has >1 venue. `staff_invites` is schema-only per build-plan's Feature 03 scope — invite-creation/acceptance LiveViews are a follow-up, not silently dropped.

**Two real bugs found and fixed only by booting the actual dev server (the 148-test suite never touched them, since `Oban testing: :manual` in test.exs means Oban's background machinery never runs there):**
1. **Oban's own `oban_jobs`/`oban_peers` queries have no per-request `org_id`** (they run in supervised background processes, not inside a web request) — they hit the new tenant guard and crashed the app in an infinite restart loop on boot. Fixed with a second, non-tenant-enforcing `Tabletap.ObanRepo` pointed at the same physical database (`lib/tabletap/oban_repo.ex`), wired into `application.ex` and `config/{dev,test,runtime}.exs` — Oban's `repo:` config now points there instead of `Tabletap.Repo`.
2. `Phoenix.Ecto.CheckRepoStatus` (dev-only) blocked every request with a pending-migrations error until `mix ecto.migrate` actually ran against the dev database — the test suite only ever touched the sandboxed test database, so this was invisible there too.

Also fixed in passing (surfaced by turning on tenant enforcement, not new bugs): every direct `Repo.*` call touching `User`/`UserToken` — in `Accounts`, `User.unsafe_validate_unique/3` (needed `repo_opts: [skip_org_id: true]`), and in the pre-existing Feature 02 test files — needed explicit `skip_org_id: true` (`users`/`user_tokens` aren't tenant-owned, code-standards.md's documented exception list). And a real pre-existing bug in the generator's `UserAuth.log_in_user/2`: `signed_in_path(conn)` was being evaluated against the *pre-login* `conn` (scope not yet refreshed) and, separately, could never match its role clause at all when called with a `%Phoenix.LiveView.Socket{}` (struct-typed pattern match) instead of a `%Plug.Conn{}` — both fixed (`user_auth.ex`), which is what makes the owner→`/dashboard` post-login redirect actually work.

122/122 → **148/148 tests green**, `credo --strict`/`format --check-formatted` clean, server boots and the full org-signup → auto-login → dashboard flow verified twice: once through `Phoenix.LiveViewTest` (`render_submit` + `follow_trigger_action` — `submit_form/2` alone doesn't fire `phx-submit`, so it can't be used here the way it can for a plain login form) and once by running `Tenants.create_org_with_owner/1` + `build_scope/2` directly against the live dev database and inspecting the real rows (no `chromium-cli`/`playwright` available in this environment for a screenshot-based check).

</details>

**Next:** Feature 20 commit 2 — wire push into the 3 real events + Q28's in-app audio alert. Full 5-commit plan: `.claude/plans/shiny-mixing-donut.md`.

**Working tree note — resolved (2026-07-20):** the concurrent-session `Plans`/`PlanHooks` scaffolding flagged as "unreviewed" at the end of Feature 18 has now been audited (Feature 19 Commit 1) and found correct against pricing.md/build-plan.md — see the Feature 19 progress entry above for what was confirmed. No more unreviewed working-tree state from that session as of this commit.

**Flagged, not blocking:** the cross-venue monthly-spend row on `/me/history` renders under the app-default locale (`:en`) rather than any particular venue's — a deliberate call (no single venue to derive one from), not a bug, but worth knowing if a future i18n pass tries to make every money amount on a page agree.

**Flagged, not blocking:** `Public.MenuLive`'s "venue not found" path (Feature 04) has the same latent bug Feature 06 found and fixed in `Public.TableController` — it `put_flash`s then redirects to `/`, but the root layout has no sitewide `<.flash_group>` (by design — it would double-render on every LiveView page), so that flash is silently never shown. Low priority (an unknown venue slug is a much rarer path than a scanned QR — no real link points at a bad slug), but worth the same fix (a small direct not-found render) whenever that LiveView is next touched.

**Flagged, not blocking:** the POS's own wallet-assisted charges (`Payments.charge_order/3`, reused verbatim from Feature 09) never set `cashier_membership_id` on the resulting payment — only cash/comp settlements do (`Payments.settle_cash_now/3`/`charge_comp/4`). `Payments.cashier_summary/3`'s transaction list and cash total are therefore complete for cash+comp but silently exclude any wallet sale a cashier rang up on a customer's behalf. Low priority (wallet-assisted POS sales are the minority case; the shift summary's *cash* reconciliation, the number that actually matters for drawer accountability, is unaffected), but worth widening if a future feature needs a cashier's true total transaction count.

---

## Progress

### Phase 1 — Foundation — **CLOSED**
- [x] 01 Project Setup (Phoenix 1.8, deps, Oban queues, money type, CI)
- [x] 02 Auth & Scopes (magic-link auth, Scope struct, role hooks)
- [x] 03 Tenancy Core (orgs/venues/memberships, tenant-enforcing Repo, org signup)

### Phase 2 — Catalog & Floor — **CLOSED**
- [x] 04 Menu Builder (categories, items, photos, availability, daily limits)
- [x] 05 Modifier Groups (min/max/required, price deltas, attach to items)
- [x] 06 Tables & QR Codes (CRUD, token rotation, print sheet, public `/t/:qr_token` route)

### Phase 3 — Ordering Loop ← make-or-break — **CLOSED**
- [x] 07 Customer Menu & Cart (browse, modifier sheet, cart, guest token)
- [x] 08 Orders & State Machine (snapshots, transitions, atomic daily-limit reserve, tracker + ETA)
- [x] 09 Wallet Payments (Provider behaviour + WaafiPay adapter, encrypted merchant credentials, push-PIN charges, callback + reconciliation poller, refunds, platform-fee ledger)
- [x] 10 Waiter Assignment & Waiter App (shifts, Presence, lowest-load assignment, claim board, call-waiter)
- [x] 11 Serve Confirmation & Stock Deduction (QR scan to serve, deduction movements, auto-close)

### Phase 4 — Inventory — **CLOSED**
- [x] 12 Ingredients & Recipes (units, BOM editor, modifier ingredient deltas)
- [x] 13 Stock Ops & Alerts (restock/wastage/adjustment ledger, low-stock alerts, restock CSV)

### Phase 5 — Kitchen & POS — **CLOSED**
- [x] 14 Kitchen Display System (ticket columns, timers, delay alerts)
- [x] 15 Cashier POS (item grid, cash + pay-by-QR, shift summary)

### Phase 6 — Identity, Ratings & Analytics
- [x] 16 Customer Accounts & History (guest→account linking, cross-venue history)
- [x] 17 Ratings (per-item stars, aggregates on menu, manager feedback view)
- [ ] 18 Analytics Dashboard & Rollups (nightly rollups, KPIs, exports)

### Phase 7 — Platform & Polish
- [ ] 19 Subscriptions & Platform Admin (monthly invoice + wallet collection, tenant admin)
- [ ] 20 Notifications & PWA Hardening (web push, service worker, onboarding checklist)
- [ ] 21 Reliability & Ops Hardening (2-node cluster, Postgres HA + restore drill, stuck-order watchdog, chaos drill)
- [ ] 22 Load & Security Pass (tenant-isolation audit, load test, race tests)

### Phase 8 — Mobile Apps (React Native + Expo)
- [ ] 23 API & Auth Hardening for Mobile (token auth, deep links, push registration, contract tests)
- [ ] 24 TableTap Customer App (scan-to-order, PaymentSheet, live tracker, history, stores)
- [ ] 25 TableTap Staff App (waiter mode: queue/scan/push; owner mode: live numbers/alerts)

---

## Decisions Made During Build

_Add decisions here as they are made during implementation._

**Round-7 — PRICING RESTRUCTURED TO FEATURE TIERS (2026-07-08, design-qa.md Q63, supersedes Q62; founder delegated: "rewrite it, list them based on features someone will use," entry price set to $40/month):** Round-6's 2-tier venue-count model (both tiers got the same full feature set) is replaced with a 3-tier **feature-differentiated** ladder: **Essentials** $40/venue/month + 2.5% fee, 1 venue — order loop only (QR ordering, KDS, waiter app, cashier POS, tracker, today's live numbers). **Growth** $75/month + 1.5% fee, 1 venue — adds inventory/recipes + the full 13-report Report Center + menu engineering. **Pro** $55/venue/month + 1.0% fee, 2–10 venues — Growth depth on every venue + cross-venue comparison + org Profit rollup. Fee % now decreases with tier (not just venue count) — committing to more of the platform earns a lower take rate. Trial (Q29) reinterpreted: unlocks **every tier's features** for 14 days so a trialing venue can see what Growth/Pro actually offer before choosing. Downgrade rule (Q48) extended: Growth→Essentials is allowed (a real feature downgrade, data preserved not deleted, archive-never-delete) vs. Pro→lower blocked while venue count > 1 (unchanged shape). role-features.md rows annotated **(Growth+)**/**(Pro only)** where features are now gated. Full spec: **context/pricing.md** (rewritten). Landing page pricing section rebuilt to 3 cards.

**Round-6 — PRICING NUMBERS SET (2026-07-08, design-qa.md Q62; founder delegated: "study the pricing and decide"):** Q24/Q48/Q59 had committed to the *shape* of a Starter/Pro-by-venue-count-and-fee% model but left every number unset (the marketing page could only say "a small per-order fee" — deliberately, per research/landing-page-patterns.md). Decided: **Starter** $19/venue/month + 2.0% per-order fee, 1 venue. **Pro** $15/venue/month + 1.2% per-order fee, 2–10 venues (matches project-overview.md's stated "1–10 venues" target market exactly — no invented Enterprise tier), adds cross-venue comparison + org Profit rollup. Fee applies only to wallet/card orders, unchanged cash/comp exemption (Q24). **Monthly billing only, no annual plan** — the wallet rails have no recurring-mandate API (Q58), so an annual lump-sum PIN-approved charge would fight the "no card required" trust positioning the trial leans on. Jigjiga/ETB pricing (Phase C) deliberately left unset until that phase's legal/provider gates clear rather than fabricated now. Full numbers + mechanics + rationale: **context/pricing.md** (new file, single source of truth — architecture.md/role-features.md/build-plan.md now point to it instead of repeating figures). Landing page pricing section updated to match (`lib/tabletap_web/controllers/page_html/home.html.heex`).

**Round-5 — LAUNCH-MARKET REALITY (2026-07-07, grill session, design-qa.md Q57–Q61 — supersedes Q17):** founder is in Somalia; **MVP target cities: Hargeisa, Mogadishu, Jigjiga**. Stripe operates in none of them → payments rebuilt on mobile-money wallets behind a **`Payments.Provider` behaviour**: **WaafiPay first** (ZAAD/EVC Plus/Sahal — REST/JSON, sandbox, refunds, HMAC callbacks **without retries** → mandatory reconciliation poller), eDahab second, **Chapa** for Ethiopia Phase C (eBirr has no public API; gated on data-localization legal opinion + Chapa spike + ETB fee-repatriation answer). Money stays venue-direct on each venue's own encrypted merchant credentials; platform fee = `platform_fee_ledger` accrual collected monthly with the subscription via push prompt (no split-payment or recurring-mandate APIs exist). No chargebacks, no card minimums on wallet rails. Currencies **USD (Somali cities) + ETB (Jigjiga)** — per-currency org rollups. Locales Somali/English/Arabic — **RTL is a day-one feature**. Onboarding promise revised: "first order <1hr **once merchant credentials exist**" (WaafiPay onboarding is manual paperwork we hand-hold). Phasing A/B/C proposed (pilot city → second Somali city → Jigjiga); pilot city itself still unconfirmed (grill Q4 open). Research: context/research/somalia-payments-waafipay-zaad.md + ethiopia-payments-ebirr.md.

**Round-4 review tail — 10 fixes, paper review CLOSED (2026-07-07, design-qa.md Q47–Q56):** Founder decided: **passwords required for owner/manager** (email delay can never lock a venue out; waiters stay magic-link-first), **plan downgrade blocked** until venue count fits the target cap, **solo-waiter auto-accept in MVP** (one waiter on shift = no 90s window, watchdog-only alerts). Fixes: real transactional email provider (Postmark/SES + SPF/DKIM) + per-address magic-link throttle (Q47); **DB-backed carts** — `carts`/`cart_items` tables, rebuilt on mount, deploys can't wipe carts (Q50); webhook fetch-and-reconcile, never trust event snapshots (Q51); scheduled report emails re-authorize membership at send time (Q52); venue currency immutable after first order (Q53); dispute-evidence subset retained 180 days post-offboarding for the chargeback window (Q54); ~30s Presence flap grace + flap telemetry (Q55); checkout refund-policy line + browser floor iOS Safari 15+/evergreen Chrome with honest update page (Q56). **Verdict recorded: further paper review has diminishing returns — remaining unknowns live in running code (Verify steps + Phase-7 chaos drills).**

**Round-3 adversarial review — 17 fixes (2026-07-07, design-qa.md Q30–Q46; founder delegated: "research the best way"):** **Comps allowed** (Q30: zero-total → `payments.provider: comp`, manager-gated, reason required, stock still deducts — industry comp-vs-void discipline; the kitchen rule is now "no order without a recorded settlement: Stripe/cash/comp"); **closed-venue history stubs** (Q31: offboarding archives account-holders' orders as anonymized platform-level stubs); **pickup no-show flow** (Q32: `ready` > `pickup_timeout_minutes` default 15 → flag → manager resolves). Fixes: per-token cap is the real rate limit, IP only throttles token minting (Q33); Stripe-minimum checkout gate (Q34); over-refund guard with payment-row lock (Q35); discounts pre-payment only (Q36); refunds count on refund's business day (Q37); post-close adjustments instead of silent report mutation + rollup recompute (Q38); order numbers key on business date (Q39); trial/subscription expiry enforced at next cutoff (Q40); archive-never-delete for items/categories/ingredients/tables (Q41); checkout revalidates modifier structure (Q42); stocktake snapshots theoretical at start (Q43); membership deactivation force-ends shifts + hands off orders (Q44); shifts auto-close at cutoff, flagged (Q45); pickup call-waiter → "Ask at the counter" (Q46). Plus a 20/line cart quantity cap.

**Round-2 adversarial review — 13 fixes (2026-07-07, design-qa.md Q17–Q29):** Founder decided: **launch market = Stripe-supported country** (Stripe Connect stays as designed; non-Stripe/MENA expansion = provider-abstraction project, re-open before signing venues there); **pickup fulfillment mode in MVP** (`venues.fulfillment_mode` — counter-service venues skip the waiter loop, customer collects on `ready` via tracker-QR scan); **iOS staff push risk accepted** (install-to-home-screen required at onboarding + audio alerts; staff app stays Phase 8); **14-day free trial, no card** (`orgs.trial_ends_at`, live ordering during trial, billing wall on expiry). Recommended fixes applied: manager-only manual serve confirm (Q19), `venues.business_day_cutoff` default 4am (Q20), late-3DS resurrection-or-auto-refund (Q21), cash refunds netted from drawer (Q22, `stripe_refund_id` nullable + `refunds.status`), `debit_negative_balances` + loud refund-failure alerts (Q23), zero fee on cash = acknowledged pricing decision with cash-share monitoring (Q24), one-step-back KDS undo (Q25), cashier Revive of expired cash orders (Q26), 86 flags open tickets (Q27).

**Pre-implementation decisions (2026-07-06, from research):**
- **FK multi-tenancy over schema-per-tenant** — one DB, `org_id` everywhere, enforced by `Repo.prepare_query` raise + Phoenix 1.8 Scopes + composite FKs. Schema-per-tenant rejected: migration cost per tenant, poor fit with cross-venue customer history.
- **PWA-first, no native apps at MVP** — LiveView Native was archived Feb 2026; customer QR flow shouldn't require an install anyway. JSON API `/api/v1` built alongside so native apps can come later.
- **Stripe Connect direct charges** (not destination charges) — customer pays the venue's own account, statement shows venue name, platform income = `application_fee_amount` + Stripe Billing SaaS subscription. Platform never holds venue money.
- **Hold daily-limit stock at checkout (`pending_payment`, 12-min TTL), deduct ingredient stock at `served`** — holds before charging mean a customer is never refunded because stock ran out post-payment (supersedes the earlier reserve-at-`placed` design, which had a pay-then-unfulfillable race — see design-qa.md Q1); ingredient truth follows physical serving.
- **Waiter assignment = same-table stickiness first, then lowest open workload + round-robin tiebreak, 90s accept window, then venue-wide claim board** — one waiter owns a table per sitting (design-qa.md Q8); legible and fair; cover-count weighting deferred.
- **Owner dashboard fully specified (2026-07-06):** owner-dashboard.md defines all 7 analytics screens (Today live, Revenue & Sales, Menu Performance with menu-engineering quadrant, Feedback, Staff & Work Analytics, Inventory & Cost with food-cost % and variance, Customers) + org comparison + mobile subset + alert matrix. Discipline: every metric must be computable from the existing schema. `daily_rollups` schema was extended to carry the required fields; Feature 18 builds to this spec.
- **Founder confirmations on the gap fixes (2026-07-06):** (1) cashier = full customer proxy — anything the QR flow does, the cashier can do at the counter; cash flow locked: customer picks Cash at checkout → order number on screen → cashier enters number, takes cash, verifies → order fires. (2) Combos are composed entirely by managers (any groups/options/price). (3) **Report Center required**: report catalog × daily/weekly/monthly/yearly/custom + scheduled email — spec in owner-dashboard.md. (4) i18n/RTL groundwork kept (delegated decision).
- **Money & analysis round (founder requests, 2026-07-06):** restock report gains a print-ready purchase-order sheet (low-stock alert links straight to it); reports gain season/quarter grouping + same-period-last-year comparison; visibility rule set (manager sees all venue reports; owner-only: cross-venue, billing, fees, org profit); **Profit report (P&L-lite) added as 13th type** — net revenue − COGS = gross profit/margin + purchases/wastage/fees, honestly labeled gross-not-net (no labor/rent tracking); `stock_movements.unit_cost` added so purchases are valued at what was actually paid.
- **Report Center expanded to 12 types (founder request, 2026-07-06):** added successful-orders-&-bills (all served/closed orders with full receipt detail), payments money-in (every payment, online Stripe vs cash, reconciles to revenue to the cent), cashier daily cash (per cashier: cash taken, expected vs counted, variance), and assisted orders (staff-placed orders per cashier — new `orders.placed_by_membership_id` column supports it).
- **Café-needs gap analysis (2026-07-06):** walked a venue's full day against the docs. Four MVP gaps fixed: POS table orders (no-QR dine-in fallback), combos via the existing modifier model ("Combo" builder template, no schema change), end-of-day close/Z-report, i18n+RTL groundwork (Gettext everywhere, logical-property CSS, venue.locale). Deferred-features register with build triggers now lives in design-qa.md → Gap Analysis (incl. the fiscal-receipt compliance warning for regulated markets).
- **Adversarial design review (2026-07-06):** walked every flow hunting failure scenarios; 16 hard questions answered in **design-qa.md** (the reasoning record — read it before changing ordering/payment/assignment behavior). Headline additions: `pending_payment` hold state, Busy Mode throttling (industry standard per Toast/ChowNow), pay-at-counter option, line-item partial refunds + dispute evidence pack, 86/auto-86, stocktake variance, unserveable-order flow, manager reassign, QR anti-abuse gates, GDPR lifecycle.
- **Working title "TableTap"** — rename is a find-and-replace away; don't bikeshed before Phase 1.
- **Money-math gap-fill (second research pass, 2026-07-06):** compared docs against Toast/Loyverse/QR-payment feature sets and added the POS table stakes that were missing — attributed discounts (`order_discounts`), itemized digital receipts, POS open-ticket editing. Explicitly deferred with rationale: split/merge tickets, hold/fire coursing, offline mode.
- **Mobile stack = React Native + Expo, TypeScript (researched 2026-07-06):** customer, waiter, and owner get real mobile apps (two apps: TableTap + TableTap Staff); manager/cashier/kitchen stay LiveView web. RN chosen over Flutter because the **official** `phoenix` npm Channels client (maintained by the Phoenix team) and **official** Stripe RN SDK cover our two riskiest integrations — Flutter's `phoenix_socket` is community-run, 15 months stale, Presence experimental. Native Swift+Kotlin rejected: two apps × two platforms at 70–90% higher cost with no need (no AR/heavy graphics). QR-scan → browser PWA stays the first-order path (no install to order); apps ship Phase 8 after web MVP.
- **No taxes, tips, or service charges (founder decision, 2026-07-06):** menu prices are final; `total = subtotal − discounts`. All tax/tip/service-charge fields and flows were removed from the design. If a future market requires tax receipts or tipping, that's a deliberate re-scoping — check with the founder first.

**Feature 01 implementation decisions (2026-07-07):**
- **Postgres via `docker-compose.yml` (postgres:16 + adminer), not a native install** — founder's explicit instruction, avoids fighting the host OS's package/service setup; `db` service matches the generator's default dev/test credentials exactly (postgres/postgres@localhost:5432), so `config/dev.exs`/`test.exs` needed zero changes. Adminer at :8080 for ad-hoc inspection instead of installing a GUI client.
- **`ex_money` v6.1 dropped the compile-time CLDR backend module** (superseded by `:localize`) — confirmed via current hexdocs before writing config, since a plausible-but-wrong config here would silently misconfigure locale/currency handling. Config: `config :localize, supported_locales: [...]` + `config :ex_money, auto_start_exchange_rate_service: false` (we deliberately never convert currencies — Q53); logged in library-docs.md.
- **No Oban cron entries added yet** — `Oban.Plugins.Cron` validates referenced worker modules exist at boot; a config in Feature 01 could crash the server, so cron lines get added in the same commit as the worker they schedule (starting with `DailyRollup` at Feature 18).
- **WaafiPay hostname deliberately has no default** in `runtime.exs` — the research note flags conflicting `.com`/`.net` sources as UNVERIFIED; `System.fetch_env!` forces a real value rather than trusting a guess.

**Feature 02 implementation decisions (2026-07-07):**
- **Per-IP throttle is a hand-rolled ETS GenServer (`TabletapWeb.RateLimiter`), not a new dependency** — a fixed-window counter is the entire requirement (design-qa.md Q47b); pulling in Hammer/PlugAttack for this would be the kind of dependency code-standards.md's "does Phoenix/Elixir already do this?" check exists to prevent. Not distributed (each node has its own window) — acceptable for an abuse guard, not a security boundary; revisit if multi-node (Feature 21) shows it matters.
- **Per-email throttling lives in `Accounts.deliver_login_instructions/2`** (checks for a `UserToken` sent in the last 60s), separate from the per-IP check in the web layer — matches the context/web split (code-standards.md: web modules never own domain rules). The generator's existing "link sent if the account exists" copy on the login form already satisfied Q47's non-enumeration requirement — nothing to add there.
- **`Scope` gained `org`/`venue`/`membership`/`role` fields, all `nil` for now** — Feature 03 is what resolves them (there's no Org/Venue/Membership schema yet to resolve *from*). Writing the full struct shape now means Feature 03 extends a plug, it doesn't redefine a type other code already depends on.
- **`ScopeHooks.on_mount` clauses are named per role** (`:require_manager`, `:require_waiter`, etc.), each a thin wrapper over a shared `require_role/2` — satisfies both build-plan.md's "require_role/2" primitive and code-standards.md's named-hook convention. Every clause denies everyone right now, correctly, since `role` is always `nil` until Feature 03.
- **Owner/manager required-password enforcement (Q47a) deferred to Feature 03**, not skipped — the rule is about *who* (a role) must set a password *at* an account-creation flow (org signup / staff invite) that doesn't exist until Feature 03. Flagged in "Next" above so it isn't forgotten.

---

## Known Technical Risks

| Risk | Mitigation |
|---|---|
| Cross-tenant data leak | Three-layer enforcement (Scope, `prepare_query` raise, composite FKs); dedicated audit in feature 22; per-context second-tenant tests |
| Overselling the last portion under concurrent checkouts | Atomic `UPDATE ... WHERE sold_qty < limit_qty` inside the order transaction; concurrency test in CI |
| Webhook delay leaves customer on "confirming…" | Optimistic confirming state + PubSub push the instant the Oban job lands; alert if p95 webhook→placed exceeds 10s |
| Waiter phone dies while assigned | Presence liveness required for assignment; 90s escalation job reroutes; off-shift-with-open-orders forces handoff |
| Stale menu cached by PWA service worker | Network-first strategy for menu/order routes; SW caches only static assets |
| Kitchen tablet browser memory over a 12h shift | Streams (bounded DOM), `TicketTimer` ticks client-side, nightly reload prompt |
| Wallet merchant onboarding (manual, in-person) blocks a venue's launch | Onboarding checklist with the guided "get your merchant account" step; ordering disabled with a clear banner until the verification charge sets `charges_enabled`; we hand-hold the pilot's paperwork |
| WaafiPay callbacks are not retried — a dropped callback could strand a paid order | Reconciliation poller on every pending payment (~30s + pre-expiry sweep) is the guaranteed confirmation path; callback-vs-poller race test in CI |
| WaafiPay unknowns (hostnames, preauth window, payout API, fees) | Flagged UNVERIFIED in research note — confirm directly with WaafiPay before Feature 09; sandbox spike first |
| Ethiopia leg (Chapa/ETB/data-localization Proclamation 1321/2024) | Phase C gated on legal opinion + Chapa test-mode spike + ETB fee-collection decision — Somali cities launch first |
| Unit-conversion mistakes corrupt inventory | Stock stored in base units only; conversions at input boundary with live hint; movements ledger is append-only and re-derivable |
| One venue's traffic spike affecting others | Single BEAM handles it for MVP scale; per-venue PubSub topics; load test at feature 22 defines the real ceiling |
| App down at the moment a customer pays | Stripe retries webhooks up to 72h; idempotent processing; order materializes on recovery — delayed, never lost (see architecture.md Reliability section) |
| Order silently stuck (waiter gone, kitchen missed it) | 90s escalation → claim board; KDS overdue pulse; Oban cron watchdog alerts managers on any non-terminal order past thresholds |
| Whole-node or DB failure | 2-node cluster + LiveView auto-reconnect; managed Postgres failover with PITR; chaos drill in feature 21 proves recovery without order loss |
| ETA wildly wrong early on (no history) | Fall back to static `prep_minutes`; label as "~estimate"; rolling average kicks in after 20 served orders per item |

---

## Notes

_Add notes here as the build progresses — workarounds, patterns, anything that differs from the context files._

- `requirment.md` in the repo root is the founder's original draft — superseded by these context docs but kept for reference. Features it lists that we deliberately deferred: PDF export (CSV first), WhatsApp integration, loyalty program, table booking, payroll.
- **Marketing home page built (2026-07-07), then rebuilt same day against category research** (not a Feature 01–25 item — logged here, not the checklist above). Research: fetched and dissected the live landing pages of sunday, me&u, Owner.com, and TouchBistro plus NN/g fold/value-prop research → `context/research/landing-page-patterns.md` (Toast 403s bots; Square's page is a JS shell — both flagged UNVERIFIED; the background research agent died on a session limit mid-run and the note was finished inline). Finding: the whole category is sales-led — pricing hidden on every page checked, "Talk to sales"/"Get a quote" CTAs, logo walls + usage stats right under the hero. The rebuild copies the invariant skeleton (outcome hero → proof strip → mechanism → audience-segmented sections with product mockups) and inverts what we can win on: self-serve trial CTA ("Start free — 14 days, no card"), a transparent pricing-model section positioned against "talk to sales", and **zero fabricated social proof** (pre-launch: no logos/testimonials/stats — the guarantee strip is explicitly labeled "Engineered promises — not marketing numbers"). Key structures: quantified-outcome hero ("From table scan to paid order in 90 seconds" — sunday's headline formula, our real Success-Criteria number), a two-screen hero visual showing the SAME order #248 on the guest tracker and a spec-accurate dark KDS ticket ("what the guest sees / what the kitchen sees"), sunday-style For-guests/For-staff/For-owners sections each with a ui-tokens.md-accurate component mockup (menu card + cart bar, waiter NEXT-UP card, owner stat tiles), wallet badges (ZAAD/EVC Plus/eDahab) in the hero, and a founding-venue final CTA replacing testimonials. Logical CSS properties (ms-/ps-/border-s) per ui-rules.md's RTL rule. Verified: compile/format/credo/tests green (122), server boots, every section + heroicons confirmed in rendered HTML and compiled CSS.
- **Marketing page visual redesign pass (2026-07-08, `/ui-ux-pro-max` skill)** — same blueprint/copy, upgraded visual system. The skill's generated palette (appetizing red) and "App Store" pattern (star ratings) were **rejected** — brand tokens (#C96F2E) are locked in ui-tokens.md and ratings are fabricated social proof; its typography find was adopted: **Calistoga display serif** for headings (warm/food-adjacent, rated for B2B SaaS), **self-hosted** at `priv/static/fonts/calistoga-latin.woff2` (41KB, no Google-CDN fetch — unreliable networks in the launch market) with `font-display: swap`, exposed as the Tailwind `font-display` utility. Scoped to marketing only — ui-tokens.md's "no display font at MVP" still governs product surfaces; Calistoga ships only weight 400 so display headings use `font-normal` (never `font-bold` → synthetic bold). Other changes per skill rules: skip-to-content link; mobile nav dropdown (was: anchors just hidden below md); phone frame around the hero guest tracker; mono uppercase eyebrows on every section; flat-design elevation discipline (borders + shadow-sm everywhere, shadow-xl reserved for the two hero devices); `motion-safe:animate-ping` + reduced-motion-guarded `scroll-behavior: smooth` + `scroll-mt-20` on anchor targets; new "Everything included" 6-tile feature grid (blueprint §6, previously skipped); footer nav links; nav buttons bumped from btn-sm toward 44px touch targets. Verified: compile/format green, 122 tests, font serves 200 via `/fonts/`, all new sections render.
