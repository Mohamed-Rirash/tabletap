# Memory — TableTap: Phases 1-6, Features 19 & 20 CLOSED. Feature 21 (Reliability & Ops Hardening) in progress, 1/5 commits.

Last updated: 2026-07-21 (same session as Features 14/16/17/18/19/20: round-4 tail fixes `afe679c`, Feature 14 `81259be`, Feature 15 `79e0789`, Feature 16 `7c582ab`, Feature 17 `26d830e`, Feature 18 all 6 parts `97febd0`/`2ad60c1`/`024e181`/`738e064`/`0628638`/`e15a467`/`3fc68b4`, Feature 19 all 5 parts `465da06`/`c692fa6`/`56aed9f`/`999da23`/`257c703`, Feature 20 all 5 parts `4b8b3cb`/`7d7a449`/`56b15a5`/`2c56667`/`6824e46`, Feature 21 commit 1 pending commit)

## What was built

Since the last memory save (which ended at Feature 03), Features 04–17 all shipped, one commit each on `main`. Feature 18 (Analytics Dashboard & Rollups) is the largest feature in the plan by far — 7 dashboard screens + org comparison + a 13-report Report Center — and is being built as **several staged commits within one feature**, a deliberate deviation from every prior feature's one-commit norm, confirmed with the user given the size. The authoritative per-feature/per-commit narrative lives in `context/progress-tracker.md`; do not duplicate it here. Highlights:

- **Phase 2 — Catalog & Floor (04–06):** `Tabletap.Catalog`, modifier groups, tables & QR.
- **Phase 3 — Ordering Loop (07–11):** `Tabletap.Ordering` (carts/orders/state machine), `Tabletap.Payments` (WaafiPay), waiter assignment, serve confirmation.
- **Phase 4 — Inventory (12–13):** `Tabletap.Inventory` full CRUD/UI, restock/wastage ledger, auto-86, stocktake.
- **Phase 5 — Kitchen & POS (14–15), CLOSED:** Kitchen Display System; Cashier POS.
- **Phase 6 — Identity, Ratings & Analytics, CLOSED:** Feature 16 (accounts/history), Feature 17 (ratings), Feature 18 (Analytics Dashboard & Rollups — nightly rollup job, all 7 owner-dashboard.md screens, org comparison, 13-report Report Center with scheduled email delivery).
- **Phase 7 — Platform & Polish, Feature 19 (Subscriptions & Platform Admin) CLOSED:** all 5 staged commits landed, same discipline as Feature 18. Commit 1 (feature-gating retrofit): wired the already-built-but-unused `PlanHooks`/`require_*_feature` gating into `/reports*` and `/analytics/venues`, closing a TODO the concurrent Feature 19 scaffolding left in `router.ex` at the end of Feature 18. Commit 2 (billing screen + trial banner + add-venue): `Manager.BillingLive` (plan/price, itemized preview, plan-change, add-venue), `Tenants.create_venue/2` + `change_plan/2`, `Payments.unsettled_platform_fees_by_currency/1`, shared `<.trial_banner>` in `CoreComponents`. Commit 3 (billing engine): `Tabletap.Billing` (30-day period billed in advance from trial end, `collect_invoice/1` pushes the actual WaafiPay charge against platform credentials, settles ledger rows, drives `subscription_status` — active/past_due/canceled two-strikes), nightly `Billing.Workers.CollectMonthlyInvoices`, `trial_banner` evolved into `subscription_banner` (4 states) and moved into `Layouts.manager` so it's app-wide, `Public.MenuLive`'s `:unavailable` render clause for a canceled org's QR menu. Commit 4 (platform admin panel): `users.platform_admin` flag, `TabletapWeb.AdminAuth`, new `Tabletap.Admin` context (first-ever legitimate `skip_org_id: true` context outside `Accounts`/`Tenants`), `/admin` + `/admin/tenants/:id` LiveViews, strictly read-only. Commit 5 (GDPR data lifecycle, final): `Accounts.delete_account/1` (nearly free — `orders`/`item_ratings`' `customer_user_id` FKs already `nilify_all` from Feature 16/17), tenant offboarding via a new `Tabletap.Offboarding` context (90-day archive-then-hard-delete, 180-day dispute-record retention via two new flat denormalized tables, not surgical per-table deletes), `Manager.OrgExportController`, `BillingLive`'s new danger zone. 814 tests green across the whole feature.
- **Phase 7, Feature 20 (Notifications & PWA Hardening) CLOSED:** staged 5 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. Commit 1 (Web Push infrastructure): `{:web_push_ex}` was already a dep, unconfigured — real VAPID keys generated (`mix web_push_ex.vapid`), same public/private config split as WaafiPay platform creds. New `push_subscriptions` table + `Tabletap.Notifications` context (not tenant-owned, `skip_org_id: true` throughout — same reasoning as `Accounts`), service worker, JS hook, opt-in button on `Waiter.QueueLive`/`Manager.DashboardLive`. Commit 2 (push wiring + Q28 audio alert): new `Notifications.Workers.SendPush` enqueued from all 3 existing PubSub broadcast points (`Ordering.assign_to/3`, `Ordering.call_waiter/2`, `Inventory.broadcast_if_low_stock/3`), no new domain events needed; new `assets/js/hooks/audio_alert.js` Web Audio oscillator beep wired into `Waiter.QueueLive`. Also fixed a real test-infra bug found along the way (see Problems solved) — `Tabletap.ObanRepo` wasn't sandboxed, so `Oban.insert/1` calls were committing for real across the whole test suite's history. Commit 3 (PWA manifests + offline shell + install prompts): new `TabletapWeb.PwaHooks` (`on_mount`, mirrors `ScopeHooks`) assigns `:pwa_manifest` (`"customer"` for the `:public_menu` live_session, `"waiter"` for `:waiter`) — `root.html.heex` reads it to conditionally link `<link rel="manifest">`; two new `priv/static/manifest-{customer,waiter}.webmanifest` files (only icon is the existing `logo.svg`, no PNG asset available). New `assets/js/pwa.js` (plain JS, not a phx-hook — the offline banner/install button live in the root layout's static shell, outside any LiveView's managed subtree, where hooks never initialize) drives an offline/reconnect banner (`navigator.onLine` + `liveSocket.socket.onError/onOpen`) and a `beforeinstallprompt`-driven install button (Chromium-only; no-op on Safari by design). Commit 4 (browser floor Q56 + iOS onboarding block Q28): new `TabletapWeb.BrowserFloorPlug` (first in `:browser`, conservative UA sniff — Trident/MSIE, iOS OS 14 and below, Chrome major version below 100 — erring toward letting anything unrecognized through), redirects to a new plain non-LiveView `/unsupported-browser` page (`BrowserFloorController`). iOS onboarding: new `ios_install_gate.js` hook + `Waiter.QueueLive`'s `<.ios_install_gate>` — a full-screen overlay revealed only client-side via `navigator.standalone`, no server round trip. Commit 5 (onboarding checklist widget, final): new `Analytics.onboarding_checklist/1` — no new schema, derives `venue_info`/`wallet_setup`/`menu`/`tables`/`first_order` from data that already exists (new `Ordering.any_order_placed?/1` for the last one); `Manager.DashboardLive` shows it only while incomplete. 848 tests green — Feature 20 done.
- **Phase 7, Feature 21 (Reliability & Ops Hardening) in progress:** staged 5 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. Confirmed with the user: reuse the already-scaffolded `DNSCluster` instead of adding `libcluster` (build-plan.md names it literally, but DNSCluster already exists and does the same job); for the genuinely infra-only pieces (managed Postgres HA, a real 2-node deploy), document + run a local proof where honestly possible rather than fake it. Commit 1 (clustering + verification): discovered `config :tabletap, :dns_cluster_query, System.get_env("DNS_CLUSTER_QUERY")` **already existed** in `runtime.exs` (original Phoenix scaffold) — just documented it (comments in `application.ex`/`runtime.exs`, `architecture.md`'s Deploy row) rather than adding new code. Then **genuinely verified cross-node PubSub locally**: booted two separate BEAM nodes (`elixir --sname nodea`/`nodeb`, same cookie, both running the full app via `mix run`), `Node.connect/1`'d them, confirmed via `:pg.get_members/2` both nodes' local PubSub workers synced into one group, then confirmed a real `Phoenix.PubSub.broadcast/3` from node B was received by a subscriber on node A.

## Decisions made

All build decisions are recorded in `context/progress-tracker.md` and `context/design-qa.md` (Q1–Q63) — those are the source of truth, not this file. Feature 18's own cross-cutting decisions (payment-mix scope, food-cost-estimate vs accurate figure, rollups-never-closed, staff_metrics scope) live in `Tabletap.Analytics`'s own moduledoc — read that first before touching any Analytics function.

## Problems solved

Per-feature bug narratives are in the tracker. Durable environment/infra lessons worth keeping here:

- Playwright IS usable in this environment: `npm install playwright` in a scratch dir picks up the cached Chromium at `~/.cache/ms-playwright` (no download). `/dev/mailbox` (Swoosh local adapter) is real and scriptable too.
- `Repo.prepare_query/3` injects `org_id` into **every** query it runs through the tenant-enforced Repo, including preload sub-queries — `preload: :user` off a tenant-scoped query crashes because `users` has no `org_id` column. Fix: two explicit queries, zipped together.
- **`Repo.put_org_id/1` is ambient process state, not tied to whichever `%Scope{}` you're holding.** Any helper doing Ordering-context reads/writes should re-assert `Repo.put_org_id(scope.org.id)` as its own first line.
- Cross-tenant reads/writes belong in whichever context already owns the underlying table — loop `Tenants.list_org_ids/0` + `Repo.put_org_id/1` per org (`Workers.SweepAbandonedCarts`'s shape). Never reach for `skip_org_id: true` outside `Accounts`/`Tenants`/platform-admin.
- A `phx-trigger-action` form submit is a no-op if clicked before the LiveSocket JS attaches — add `page.waitForLoadState("networkidle")` first in Playwright verify scripts.
- Always boot the real dev server after Repo/Oban/supervision changes; `Oban testing: :manual` hides boot loops from the test suite.
- Root layout has no sitewide `<.flash_group>` — a controller `put_flash` + `redirect` is silently invisible.
- Never hard-match `Decimal.parse/1` on raw user-typed input — returns `:error` for anything non-numeric.
- **`mix compile`/`mix test` can silently start compiling a whole Erlang toolchain from source** if `.tool-versions` names a version `mise` doesn't have cached. Check `asdf list erlang` first; prefix commands with `PATH="/home/rirash/.asdf/shims:$PATH"`.
- `<.money>` with no `locale=` falls back to the app default (`:en`) — differs visibly from a venue-scoped call on the same page. Know which one a row uses before asserting exact formatted text.
- **`timestamps(type: :utc_datetime)` (second precision) means two rows in the same wall-clock second are a real tie for `order_by: [desc: :inserted_at]`.** Always add a secondary sort key. When a test also needs to assert exact order, sort its own "expected" list by the *identical* composite key (`{DateTime.to_unix(inserted_at), id}`, not just `id` alone) — Feature 17's own tie-break test had exactly this bug (assumed a tie, broke when timestamps happened to differ), fixed in Feature 18 part 4a.
- Elixir requires every `def handle_event/3` (or same-name/arity function) clause to be contiguous — a `defp` between two clauses is a compile error under `--warnings-as-errors`. Recurred 3+ times this project; always append new private helpers *after* the full clause group.
- A brand-new venue has `charges_enabled: false` and `pay_at_counter_enabled: false` — a verify script must flip `pay_at_counter_enabled` on before a guest can check out.
- Phoenix's `<.button>` component doesn't render `type="submit"` — a raw `<button type="submit">` does. Check which one a template uses before writing a Playwright/test selector.
- **A raw `Date`/`DateTime` struct can't go into a `~p"...?#{[key: value]}"` verified-route query string** — `Phoenix.Param` has no impl for it, raises `ArgumentError` ("structs expect an :id key"). Always `Date.to_string/1` first. Bit every new Analytics LiveView's CSV-export link and range picker this session.
- `Enum.sort_by/3`'s `{:asc/:desc, Module}` sorter works for any module exposing `compare/2` returning `:lt/:eq/:gt` — confirmed both `Money.compare/2` (same-currency only) and `Decimal.compare/2` satisfy this, so `{:desc, Money}`/`{:desc, Decimal}` are safe, idiomatic sorters throughout Feature 18's Analytics code instead of hand-rolled comparators.
- **This session hit a live concurrent-edit collision**: another session was working on Feature 19 (Subscriptions & Platform Admin) in the *same checkout* at the same time. Several of my own uncommitted files got silently reverted to their last-committed state mid-session (lost ~30 min of Analytics/DashboardLive work, had to redo from conversation history). Lesson: if a file you just wrote comes back reading like an older version, don't assume you misremembered — check `git status`/`git diff` immediately, since concurrent writers to the same working tree are a real failure mode here, not a hypothetical. When it happens, stop and ask the user rather than guessing whether to overwrite.
- When two sessions share a working tree and a shared file (router.ex, layouts.ex) ends up with both sessions' uncommitted hunks mixed together, ask the user how to handle committing it (bundle together vs. hold out) rather than guessing — this user's call was "bundle together, don't lose either session's work."
- **Two Ecto schemas can't reference each other at compile time even indirectly** — `Reports.subscribe/3` pattern-matches `%ReportSubscription{}` (a struct literal), and `ReportSubscription`'s own schema tried `Ecto.Enum, values: Reports.report_types()` (a function call into `Reports`), and the two files deadlocked compiling each other. Fix: inline the literal atom list in the schema instead of calling back into the context module — a small, deliberate duplication rather than a real coupling.
- **`Repo.prepare_query/3`'s auto-injected `org_id` filter applies to *every* nested preload query, including ones against schemas that were never meant to be tenant-scoped** (`Org` itself — no `org_id` column, an org *is* the tenant; `User` — no `org_id` column, a user can span orgs via multiple memberships). Any preload or join that touches `Org`/`User` from tenant-scoped code needs its own two-query fetch with explicit `skip_org_id: true` (`Tenants.list_memberships/2` is the canonical example — read its own comment before reinventing this).
- **Swoosh test mailbox is FIFO and un-filtered at receive time** — `assert_email_sent(to: ...)` is `assert_received {:email, email}` (grabs the oldest `{:email, _}` message) *then* checks params against whichever one it got. A test fixture that itself sends email (`AccountsFixtures.user_fixture/1` does, via `deliver_login_instructions`) pollutes the mailbox ahead of the email you actually want to assert on. Drain the mailbox (`receive do {:email, _} -> ... after 0 -> :ok end`, looped) right before triggering the send you're testing.
- **`Swoosh.Attachment.new({:data, binary}, filename: ..., content_type: ...)` attaches in-memory content** (no temp file needed) — confirmed via `deps/swoosh/lib/swoosh/attachment.ex`, used for emailing a CSV built in-process rather than round-tripping through disk or an authenticated download link.
- **This environment's test DB connection pool (`pool_size: schedulers_online() * 2` = 8 on this 4-core box) is smaller than some existing concurrency-hammer tests' own concurrent-task count (e.g. a "10 concurrent checkouts" test in `checkout_test.exs`).** Intermittent `DBConnection.ConnectionError`/"could not checkout the connection" failures in `checkout_test.exs`/`tenants_test.exs` are pre-existing environmental flakiness from this mismatch, not a real logic bug — reproduced across several `mix test` runs, confirmed unrelated to files touched by Feature 20 Commit 4. If this needs fixing, the target is `config/test.exs`'s `pool_size` or those tests' own concurrency count, not anything in Feature 20.
- **`phx-hook` only initializes on elements inside a LiveView's own socket-managed DOM subtree.** `root.html.heex` is the static shell around `{@inner_content}` — a `phx-hook` placed directly in the root layout (outside any `<.live_view>` root) never fires, since LiveView only discovers/binds hooks while diffing a View's own container. Anything living in the root layout itself (offline banner, install-prompt button) needs plain vanilla JS wired at page load in `app.js` instead, operating on the element by id — same shape as the pre-existing theme-toggle `<script>` and topbar wiring already in that file.
- **Manually verifying cross-node `Phoenix.PubSub` locally (two `elixir --sname` BEAM nodes) is genuinely doable and worth doing — but budget a generous receive-timeout (60–90s+) on the "listener" node, not a tight one (~15–30s).** The actual `Node.connect/1` + `:pg` group sync always completed in well under a second every time it was checked directly (`:pg.get_members/2`) — every "no message received" failure was purely because composing/running the second node's script (via several sequential tool calls: write file, sleep, run) ate more wall-clock time than the listener's own short timeout, not a real clustering defect. Confirmed by directly RPC-querying the listener node's own `Node.list()`/`:pg.get_members/2` mid-test — cluster membership was always correct; only the timing of the test harness itself was wrong.
- **A second Ecto repo pointed at the same physical database (`Tabletap.ObanRepo`, kept separate from `Tabletap.Repo` specifically so Oban's own bookkeeping tables stay outside tenant `org_id` scoping) needs its OWN `Ecto.Adapters.SQL.Sandbox.mode(_, :manual)` call in `test_helper.exs` and its own `Sandbox.start_owner!/2` in `DataCase` — the sandbox checkout for one repo module does not cover a second repo module even against the identical database.** Without it, every `Oban.insert/1` in every test ever run commits for real instead of rolling back at test end — silent, invisible until something asserts `refute_enqueued(worker: X)` with no `args:` filter (broad enough to catch stray rows from unrelated tests/runs). Found via 3375 accumulated stray rows in `tabletap_test.oban_jobs`. Any future second-repo addition in this codebase needs the same two-line treatment from day one.

## Current state

- **Phases 1–6 CLOSED.** Feature 18 commits: foundation `97febd0`, Today screen `2ad60c1`, Revenue & Sales + Menu Performance `024e181`, rich Feedback + Customers `738e064`, Staff & Work + Inventory & Cost `0628638`, Org View `e15a467`, Report Center `3fc68b4` (13 reports, CSV, scheduled email). 747 tests green via `mix test` at Commit 6 (`mix precommit`'s credo step halts on the pre-existing `Tabletap.Plans` alias-ordering finding in `payments.ex`, belonging to the concurrent Feature 19 work described below — run `mix format --check-formatted` + `mix compile --warnings-as-errors` + `mix test` separately when that finding is present, same workaround used throughout this feature).
- **No browser-based verification for Commit 6** — no Playwright/`chromium-cli` available in this environment this session (unlike earlier in the project where `npm install playwright` picked up a cached Chromium — that cache may no longer be present, wasn't re-checked). Relied on the LiveView test suite instead. Worth re-checking whether Playwright is available before assuming it isn't next time.
- **The dev Postgres database (`tabletap_dev`) was found empty at the start of this session** — only `tabletap_test` existed, `tabletap_dev` was gone. Almost certainly fallout from the previous session's Docker port-conflict fix (`docker compose rm -f db && docker compose up -d db` — a full container recreate, not just restart). Fixed with a plain `mix ecto.create && mix ecto.migrate` (dev data is disposable, wasn't seeded with anything precious). If this recurs, check `docker volume ls` for whether the compose file uses a named volume that should have survived a container recreate — it may not, which would explain the data loss.
- **Feature 19 Commit 1 landed (feature-gating retrofit)**: audited the concurrent-session `Plans`/`PlanHooks` scaffolding against pricing.md/build-plan.md and found it correct — reused as-is. Wired `PlanHooks :report_center` into a new `live_session :manager_report_center` for `/reports`, `PlanHooks :org_comparison` into a new `live_session :owner_org_comparison` for `/analytics/venues` (stacks with `:require_owner`), and a new `UserAuth.require_report_center_feature/2` for `/reports.csv`. `/analytics/revenue.csv`/`/analytics/menu-performance.csv` deliberately stay ungated (pricing.md only tiers the Report Center + org comparison, not the individual always-available screens). Nav links in `layouts.ex` gained the matching `Plans.feature_enabled?/2` guards. Wrote the first tests ever for this gating mechanism (none existed for `:inventory` either). Committed as `465da06`.
- **Feature 19 Commit 2 landed (billing screen + trial banner + add-venue)**: `Manager.BillingLive` (`/settings/billing`), `Tenants.create_venue/2` (the app's first self-serve second-venue path, blocked at the plan's `venue_cap`), `Tenants.change_plan/2` (upgrade unrestricted, downgrade blocked over cap per Q48), `Payments.unsettled_platform_fees_by_currency/1` (grouped per currency, never summed — Q53), and a shared `<.trial_banner org={}>` in `CoreComponents` (needed `use TabletapWeb, :verified_routes` added to that module — it didn't have `~p` before). Committed as `c692fa6`.
- **Feature 19 Commit 3 landed (billing engine)**: new `invoices` table (`pending`-row-first idempotency, unique `(org_id, period_start)`) + `orgs.billing_wallet_msisdn`; `platform_fee_ledger.invoice_id` upgraded from a bare uuid to a real composite FK. `Tabletap.Billing.collect_invoice/1` does the actual `WaafiPay.charge/2` against `Payments.platform_credentials/0` (new — reads `config :tabletap, :waafipay`'s `platform_*` keys, which only existed in `config/runtime.exs`'s prod-only block before this — had to add dev/test placeholder values or `Keyword.fetch!` crashed outside prod). Billing period: 30 days, billed **in advance**, first period starts at `trial_ends_at` (not signup date — a trialing org's very first bill has to land right when the free ride ends, not 30 days after signup, or a 14-day trial would get a free extra 16 days). State machine: success → `:active`; first miss → `:past_due` (architecture.md is explicit ordering keeps working here); second consecutive miss → `:canceled` (a documented judgment call — no explicit grace-day count exists in the spec). Trial-ran-out-with-no-wallet → straight to `:canceled`, no `past_due` step (Q29). `CoreComponents.trial_banner/1` → `subscription_banner/1` (4 states), moved into `Layouts.manager` so it's automatic on every back-office page — removed the now-redundant explicit calls from `DashboardLive`/`BillingLive`. `Public.MenuLive` gained a dedicated `:unavailable` render clause (multi-clause `render/1`, matching `ReportsLive.report_body/1`'s established pattern) — a canceled org's venue shows ONLY the unavailable notice, not the menu with a banner on top (unlike `:paused`/`:closed`, which stay browsable). Nightly cross-tenant `Billing.Workers.CollectMonthlyInvoices` dispatches per org's status; runs at a fixed off-peak UTC cron rather than per-venue cutoff math (same simplification `DailyRollup` already established). Committed as `56aed9f`.
- **Feature 19 Commit 4 landed (platform admin panel)**: `users.platform_admin` boolean (migration, default false, no self-serve way to set it anywhere in the app). New `TabletapWeb.AdminAuth` on_mount (`current_scope.user.platform_admin`, not a role check — an admin isn't a tenant member). New `Tabletap.Admin` context — the first module outside `Accounts`/`Tenants` to legitimately use `skip_org_id: true` (code-standards.md's own "platform-admin code" exception, confirmed by re-reading `Repo`'s moduledoc closely): `list_tenants/0`, `get_tenant/1`, `cash_share_by_venue/1` (Q24), `list_invoices/1`. Two new LiveViews (`Admin.TenantsLive` at `/admin`, `Admin.TenantLive` at `/admin/tenants/:id`) using the plain `Layouts.app` shell, not `Layouts.manager` (an admin has no org/venue to drive that sidebar's venue switcher/nav). Strictly read-only — no form, no `handle_event` writes anywhere in this commit; "impersonation guard (read-only)" from build-plan.md was interpreted as "view-only through the panel," never a session-swap mechanism, as decided in the plan file before starting. Committed as `999da23`.
- **Feature 19 Commit 5 landed (GDPR data lifecycle, final commit — Feature 19 is now CLOSED)**: `Accounts.delete_account/1` (design-qa.md Q15) turned out to be nearly free — `orders.customer_user_id`/`item_ratings.customer_user_id` already carry `on_delete: :nilify_all` from their own Feature 16/17 migrations (those migrations' own comments anticipated exactly this), so deleting the `User` row *is* the anonymization mechanism; guarded against deleting an account that holds any staff membership (`memberships.user_id` is `on_delete: :delete_all`, would silently orphan a venue). Wired into `UserLive.Settings` as a "Danger zone," gated by the page's existing sudo-mode requirement — no extra confirmation needed. New `Tabletap.Offboarding` context: rather than surgically deleting ~30 child tables while keeping an org half-alive for 90 more days to satisfy Q54's 180-day payment-retention window, the 90-day mark copies out everything worth keeping (Q31's anonymized customer order archive, Q54's payment/dispute-evidence snapshot with its own `retain_until`) into two new flat/denormalized tables with **no FK back to the org**, then hard-deletes the entire org in one `Repo.delete!` — letting the schema's existing cascade FKs do the rest instead of hand-enumerating which tables to purge. Nightly `Offboarding.Workers.PurgeOffboardedTenants` finds orgs 90+ days into offboarding and dispute records past 180 days. `Tenants.export_org_data/1` (menu/orders/ingredients CSVs, reusing `Analytics.Reports.orders_report/3` + `Reports.Csv` for orders) + `Manager.OrgExportController` (zips them via Erlang's built-in `:zip.create/2`, no new dep needed) + `Tenants.initiate_offboarding/1`, all surfaced in `BillingLive`'s new danger zone. Downgrade-side venue-cap enforcement (Q48) was already fully covered by Commit 2's `Tenants.change_plan/2` — audited, confirmed, nothing added. Committed as `257c703`. 814/814 tests green, only the pre-existing unrelated `Tabletap.Plans` alias-ordering credo finding remains — same workaround (`mix format` + `mix compile --warnings-as-errors` + `mix test` separately) every commit this feature needed.
- **Feature 20 Commit 1 landed (Web Push infrastructure)**: `{:web_push_ex, "~> 0.2.0"}` was already a mix dep (unused, unconfigured) — generated real VAPID keys via `mix web_push_ex.vapid`, split config the same way as WaafiPay platform credentials (public key + subject unconditional in `config.exs`, private key per-env: dev/test hardcoded dev-only pair, `runtime.exs` prod reads `WEB_PUSH_VAPID_PRIVATE_KEY`). New `push_subscriptions` table + `Tabletap.Notifications.PushSubscription` — **not tenant-owned**, same `skip_org_id: true` reasoning `Accounts` already documents (a subscription belongs to a person, who can hold memberships across orgs). New `Tabletap.Notifications` context (`subscribe/2`, `unsubscribe/2`, `send_push/2`, `notify_user/2`, `vapid_public_key/0`). **Caught a real Ecto bug**: `Repo.insert(changeset, on_conflict: ..., conflict_target: ...)` without `returning: true` returns the struct's own *client-generated* id (binary_id autogenerate happens in Elixir before the insert) even when the upsert actually touched a *different*, pre-existing row — a re-subscribe test caught this immediately (asserted `first.id == second.id`, got two different ids). Fixed with `returning: true`, which forces Ecto to re-read the actual persisted row. Test HTTP stubbing: no existing Mox-style seam for a single function like this, so used Req's own built-in `Req.Test` (`plug: {Req.Test, Tabletap.Notifications}` sourced from a `config :tabletap, :web_push_req_options` application-config seam) instead of building one. Service worker at `priv/static/sw.js` (root-served — added `sw.js` to `TabletapWeb.static_paths/0` — a service worker's default scope is its own path and below, so it can't live under `/assets`). Shared `<.push_subscribe_button>` component + `push_subscription.js` hook wired onto `Waiter.QueueLive` and `Manager.DashboardLive`. Discovered `Waiter.QueueLive` had **zero existing test coverage** — flagged as a pre-existing gap, only added tests for what I touched (the push wiring), not a full backfill. 824/824 tests green, only the pre-existing unrelated `Tabletap.Plans` alias-ordering credo finding remains.
- **Known open gaps (flagged in tracker):** staff-invite create/accept UI doesn't exist; `Public.MenuLive` unknown-slug invisible-flash bug; `venue.locale "so"` has no CLDR data; POS wallet-assisted sales don't set `cashier_membership_id`; `/me/history`'s monthly-spend row uses app-default locale (deliberate); Feature 18's own scope gaps (payment mix has no per-wallet-network split, per-item food cost is a recipe-base estimate not the accurate stock-movement figure, no escalation-rate metric, no per-person kitchen prep time, no "time it happened" on sold-out/86 events, no manual-serve-confirm count, no season/quarter grouping UI); Feature 19's own gap — push-token purge (Q15) was a documented no-op since no `push_tokens` table existed — **now resolved by Feature 20 Commit 1's `push_subscriptions` table** (`on_delete: :delete_all` on `user_id`, so `Accounts.delete_account/1` already purges it automatically, no extra code needed). `Waiter.QueueLive` had no test coverage before Feature 20 Commit 1 — still true beyond the push wiring itself. `.tool-versions` and `context/build-plan.md`'s stray table-reformat remain the older, unrelated, never-committed noise from before this session — still untouched.

## Next session starts with

**Feature 21 commit 2 — the stuck-order watchdog.** Full plan (all 5 commits, design rationale) is at `.claude/plans/shiny-mixing-donut.md`. New `Tabletap.Ordering.Workers.StuckOrderWatchdog` (cross-tenant Oban cron, `queue: :default`, mirrors `Payments.Workers.ReconcilePendingPayments`'s exact `reconcile_org/0` shape — loop `Tenants.list_org_ids/0` + `Repo.put_org_id/1`, then a raw `Repo.all(from o in Order, where: ...)` across every venue in the org at once, no `%Scope{}` needed). Reuse the exact same "stuck" bar `Analytics.delayed?/2` already uses (`Ordering.expected_prep_minutes/1`-based) — this is the proactive-push version of an already-agreed threshold, not a new one. For each stuck order, enqueue one `Notifications.Workers.SendPush` (new `"type" => "stuck_order"` clause, reusing `Tenants.list_manager_and_owner_user_ids/1` exactly like `"low_stock"` does) with Oban's `unique: [fields: [:args], keys: [:order_id], period: :infinity]` — alert once per stuck order, not once per cron tick (first use of Oban's `unique` option in this codebase). New cron entry in `config/config.exs`'s `Oban.Plugins.Cron` list, same commit as the worker (that file's own stated rule).

## Open questions

- Sessions between 2026-07-08 and 2026-07-15 didn't save memory — assume the tracker is complete for that gap.
- Whether the concurrent Feature 19 session will resume, or whether the next session should just finish what its scaffolding started — check `git log`/`git status` at the start of the next session before assuming the working tree matches what's described here.
