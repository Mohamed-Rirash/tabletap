# Memory — TableTap: Phases 1-7 all CLOSED. Phase 8 (Mobile Apps) — Features 23 & 24 CLOSED. Next: Feature 25 (Staff App).

Last updated: 2026-07-22 (same session as Features 14/16/17/18/19/20/21/22/23/24: round-4 tail fixes `afe679c`, Feature 14 `81259be`, Feature 15 `79e0789`, Feature 16 `7c582ab`, Feature 17 `26d830e`, Feature 18 all 6 parts `97febd0`/`2ad60c1`/`024e181`/`738e064`/`0628638`/`e15a467`/`3fc68b4`, Feature 19 all 5 parts `465da06`/`c692fa6`/`56aed9f`/`999da23`/`257c703`, Feature 20 all 5 parts `4b8b3cb`/`7d7a449`/`56b15a5`/`2c56667`/`6824e46`, Feature 21 all 5 parts `e013583`/`5f4d952`/`ca7b623`/`f5aa078`/`9e8f99e`, Feature 22 all 3 parts `e7e6aff`/`1045889`/`98b7ef0`, Feature 23 all 6 parts `b2b4e0e`/`32004b4`/`65818d9`/`4027ef2`/`7668e33`/`a4830de`, Feature 24 all 6 parts `6e5a270`/`721a393`/`b84a08b`/`9dbea36`/`c2a85dc`/`8cd0b1f`)

## What was built

Since the last memory save (which ended at Feature 03), Features 04–17 all shipped, one commit each on `main`. Feature 18 (Analytics Dashboard & Rollups) is the largest feature in the plan by far — 7 dashboard screens + org comparison + a 13-report Report Center — and is being built as **several staged commits within one feature**, a deliberate deviation from every prior feature's one-commit norm, confirmed with the user given the size. The authoritative per-feature/per-commit narrative lives in `context/progress-tracker.md`; do not duplicate it here. Highlights:

- **Phase 2 — Catalog & Floor (04–06):** `Tabletap.Catalog`, modifier groups, tables & QR.
- **Phase 3 — Ordering Loop (07–11):** `Tabletap.Ordering` (carts/orders/state machine), `Tabletap.Payments` (WaafiPay), waiter assignment, serve confirmation.
- **Phase 4 — Inventory (12–13):** `Tabletap.Inventory` full CRUD/UI, restock/wastage ledger, auto-86, stocktake.
- **Phase 5 — Kitchen & POS (14–15), CLOSED:** Kitchen Display System; Cashier POS.
- **Phase 6 — Identity, Ratings & Analytics, CLOSED:** Feature 16 (accounts/history), Feature 17 (ratings), Feature 18 (Analytics Dashboard & Rollups — nightly rollup job, all 7 owner-dashboard.md screens, org comparison, 13-report Report Center with scheduled email delivery).
- **Phase 7 — Platform & Polish, Feature 19 (Subscriptions & Platform Admin) CLOSED:** all 5 staged commits landed, same discipline as Feature 18. Commit 1 (feature-gating retrofit): wired the already-built-but-unused `PlanHooks`/`require_*_feature` gating into `/reports*` and `/analytics/venues`, closing a TODO the concurrent Feature 19 scaffolding left in `router.ex` at the end of Feature 18. Commit 2 (billing screen + trial banner + add-venue): `Manager.BillingLive` (plan/price, itemized preview, plan-change, add-venue), `Tenants.create_venue/2` + `change_plan/2`, `Payments.unsettled_platform_fees_by_currency/1`, shared `<.trial_banner>` in `CoreComponents`. Commit 3 (billing engine): `Tabletap.Billing` (30-day period billed in advance from trial end, `collect_invoice/1` pushes the actual WaafiPay charge against platform credentials, settles ledger rows, drives `subscription_status` — active/past_due/canceled two-strikes), nightly `Billing.Workers.CollectMonthlyInvoices`, `trial_banner` evolved into `subscription_banner` (4 states) and moved into `Layouts.manager` so it's app-wide, `Public.MenuLive`'s `:unavailable` render clause for a canceled org's QR menu. Commit 4 (platform admin panel): `users.platform_admin` flag, `TabletapWeb.AdminAuth`, new `Tabletap.Admin` context (first-ever legitimate `skip_org_id: true` context outside `Accounts`/`Tenants`), `/admin` + `/admin/tenants/:id` LiveViews, strictly read-only. Commit 5 (GDPR data lifecycle, final): `Accounts.delete_account/1` (nearly free — `orders`/`item_ratings`' `customer_user_id` FKs already `nilify_all` from Feature 16/17), tenant offboarding via a new `Tabletap.Offboarding` context (90-day archive-then-hard-delete, 180-day dispute-record retention via two new flat denormalized tables, not surgical per-table deletes), `Manager.OrgExportController`, `BillingLive`'s new danger zone. 814 tests green across the whole feature.
- **Phase 7, Feature 20 (Notifications & PWA Hardening) CLOSED:** staged 5 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. Commit 1 (Web Push infrastructure): `{:web_push_ex}` was already a dep, unconfigured — real VAPID keys generated (`mix web_push_ex.vapid`), same public/private config split as WaafiPay platform creds. New `push_subscriptions` table + `Tabletap.Notifications` context (not tenant-owned, `skip_org_id: true` throughout — same reasoning as `Accounts`), service worker, JS hook, opt-in button on `Waiter.QueueLive`/`Manager.DashboardLive`. Commit 2 (push wiring + Q28 audio alert): new `Notifications.Workers.SendPush` enqueued from all 3 existing PubSub broadcast points (`Ordering.assign_to/3`, `Ordering.call_waiter/2`, `Inventory.broadcast_if_low_stock/3`), no new domain events needed; new `assets/js/hooks/audio_alert.js` Web Audio oscillator beep wired into `Waiter.QueueLive`. Also fixed a real test-infra bug found along the way (see Problems solved) — `Tabletap.ObanRepo` wasn't sandboxed, so `Oban.insert/1` calls were committing for real across the whole test suite's history. Commit 3 (PWA manifests + offline shell + install prompts): new `TabletapWeb.PwaHooks` (`on_mount`, mirrors `ScopeHooks`) assigns `:pwa_manifest` (`"customer"` for the `:public_menu` live_session, `"waiter"` for `:waiter`) — `root.html.heex` reads it to conditionally link `<link rel="manifest">`; two new `priv/static/manifest-{customer,waiter}.webmanifest` files (only icon is the existing `logo.svg`, no PNG asset available). New `assets/js/pwa.js` (plain JS, not a phx-hook — the offline banner/install button live in the root layout's static shell, outside any LiveView's managed subtree, where hooks never initialize) drives an offline/reconnect banner (`navigator.onLine` + `liveSocket.socket.onError/onOpen`) and a `beforeinstallprompt`-driven install button (Chromium-only; no-op on Safari by design). Commit 4 (browser floor Q56 + iOS onboarding block Q28): new `TabletapWeb.BrowserFloorPlug` (first in `:browser`, conservative UA sniff — Trident/MSIE, iOS OS 14 and below, Chrome major version below 100 — erring toward letting anything unrecognized through), redirects to a new plain non-LiveView `/unsupported-browser` page (`BrowserFloorController`). iOS onboarding: new `ios_install_gate.js` hook + `Waiter.QueueLive`'s `<.ios_install_gate>` — a full-screen overlay revealed only client-side via `navigator.standalone`, no server round trip. Commit 5 (onboarding checklist widget, final): new `Analytics.onboarding_checklist/1` — no new schema, derives `venue_info`/`wallet_setup`/`menu`/`tables`/`first_order` from data that already exists (new `Ordering.any_order_placed?/1` for the last one); `Manager.DashboardLive` shows it only while incomplete. 848 tests green — Feature 20 done.
- **Phase 7, Feature 21 (Reliability & Ops Hardening) CLOSED:** staged 5 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. Confirmed with the user: reuse the already-scaffolded `DNSCluster` instead of adding `libcluster` (build-plan.md names it literally, but DNSCluster already exists and does the same job); for the genuinely infra-only pieces (managed Postgres HA, a real 2-node deploy), document + run a local proof where honestly possible rather than fake it. Commit 1 (clustering + verification): discovered `config :tabletap, :dns_cluster_query, System.get_env("DNS_CLUSTER_QUERY")` **already existed** in `runtime.exs` (original Phoenix scaffold) — just documented it (comments in `application.ex`/`runtime.exs`, `architecture.md`'s Deploy row) rather than adding new code. Then **genuinely verified cross-node PubSub locally**: booted two separate BEAM nodes (`elixir --sname nodea`/`nodeb`, same cookie, both running the full app via `mix run`), `Node.connect/1`'d them, confirmed via `:pg.get_members/2` both nodes' local PubSub workers synced into one group, then confirmed a real `Phoenix.PubSub.broadcast/3` from node B was received by a subscriber on node A. Commit 2 (stuck-order watchdog): new `Tabletap.Ordering.Workers.StuckOrderWatchdog` (cross-tenant Oban cron, every 5 min, mirrors `ReconcilePendingPayments`'s shape exactly) reuses `Analytics.delayed?/2`'s existing "delayed" bar as a proactive push instead of a passive dashboard read; `Notifications.Workers.SendPush` gained a shared `when type in ["low_stock", "stuck_order"]` clause (identical manager/owner audience, no new code). Each order alerts once ever via Oban's own `unique` option. Found two real Oban `unique` gotchas along the way (see Problems solved). Commit 3 (degradation banners): new `Tabletap.Payments.GatewayHealth` (GenServer + ETS consecutive-failure counter, same not-distributed looseness `RateLimiter` already documents), wired into `Payments.resolve_charge_result/2`'s existing dispatch clauses — a real gateway response (approved, business decline, provider error code) always means "up"; only `:timeout` (split into its own clause) and the ambiguous/dropped-connection bucket count as a failure. New `<.gateway_degraded_banner>` rendered by `Layouts.manager` itself (computed as a cheap ETS-read assign right in `Layouts.manager/1`, before the template) — reaches every manager page with zero per-LiveView wiring. Also did the audit half of this commit: empirically confirmed (throwaway test) that Feature 20's `#offline-banner` already renders on all 5 surfaces, since every route shares the same root layout — nothing needed building there. Commit 4 (alerting telemetry): `Payments.confirm_approved/3`/`confirm_failed/2` gained an optional `via` param (`:charge`/`:webhook`/`:poller`) threaded through the shared `with_locked_pending_payment/3` wrapper, emitting `[:tabletap, :payment, :confirmed]` with `lag_ms` on every genuine resolution; new Oban-queue-depth periodic measurement (guarded against the poller's first tick racing `ObanRepo`'s own startup, and disabled entirely in test via a new config flag — Oban runs `testing: :manual` there anyway). New `Tabletap.Ops.check_order_flow/0` + `GET /healthz/order-flow`: a lazily-created, well-known-slugged synthetic org/venue (new `orgs.synthetic` column, excluded from `Admin.list_tenants/0`) running the exact real menu-load path a customer's QR scan takes. Hit and fixed two real bugs building it (see Problems solved): a `Repo.get_by/3` argument-shape mistake, and the "no `org_id` column on `Org`" preload gotcha this codebase had already documented elsewhere but I still tripped on fresh. Verified live against a booted dev server before writing the automated tests (fresh-creation, idempotent reuse, and admin-exclusion all confirmed by hand). Commit 5 (ops runbook + real local restore drill, final — **Feature 21 done**): new `docs/ops/reliability-runbook.md` — production config a human operator sets once real infra exists (Fly.io clustering specifics, managed-Postgres-HA settings tied to `Offboarding`'s own 90/180-day windows), honestly framed as guidance, not a claim. Then **actually ran** a real `pg_dump`/`DROP DATABASE`/`pg_restore` cycle against the local dev Postgres — marker row inserted first, backup timed at 0.28s, database genuinely dropped, restore timed at 1.36s, all 43 tables + the exact marker row verified intact afterward. Explicitly labeled as a downscaled mechanism proof, not a real managed-HA failover/PITR drill — the runbook's own closing section names the gap.
- **Phase 8, Feature 23 (API & Auth Hardening for Mobile) CLOSED — Phase 8's foundation is in place:** staged 6 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. Genuine from-scratch build (unlike most of 19-22) — no `/api/v1`, bearer auth, or Channels existed before this session. Commit 1 (auth foundation): hybrid bearer auth confirmed with the user over a fully-stateless alternative — short-lived (15min) stateless `Phoenix.Token` access token + a DB-backed refresh token reusing `Accounts.UserToken`'s existing hashed-token machinery with a new `"api_refresh"` context (real server-side revocation, mirrors `"session"`/`"login"` exactly). Magic-link login reused verbatim via a `tabletap://` deep link instead of a web URL — `deliver_login_instructions/2`'s `magic_link_url_fun` param already existed precisely for this seam. Commit 2 (customer API): menu/cart/checkout/tracker, guest-token based (no bearer auth — matches the web PWA's own guest-first design). New `Api.GuestScope`/`Api.Serializers`/`Api.Params.cast_uuid/1` (the last one guards every client-supplied id — a JSON API is the first surface in this codebase exposed to fully arbitrary input; an uncast `binary_id` crashes with `Ecto.Query.CastError` instead of a clean 404). Real bug caught by tests: checkout response built from `Ordering.checkout/2`'s raw un-preloaded multi-insert result, fixed by reloading via `get_order/2` first. Commit 3 (Phoenix Channels): `ApiSocket` + `OrderChannel`/`WaiterChannel`/`VenueChannel`, relaying the exact PubSub topics LiveViews already use — confirmed by grep before writing any channel code. Two real bugs: a channel topic needed the same `cast_uuid` guard as REST params, and a venue-membership authorization query initially excluded every owner (`venue_id: nil`, org-wide) and a naive one-line fix would have leaked cross-org owner access — fixed by joining the venue and only matching a nil-`venue_id` membership when its own org actually owns that venue. Commit 4 (staff API): waiter accept/serve + owner dashboard, bearer-protected via new `ApiAuth.assign_scope/2` (builds `current_scope` via `Tenants.build_scope/2`, the same function the web session uses) + `require_api_role/2` (mirrors `ScopeHooks`' per-LiveView role gates). Commit 5 (Expo push): new `device_push_tokens` table (separate from `push_subscriptions` — Expo's one-opaque-string model shares nothing with Web Push's endpoint/p256dh/auth triple), `notify_user/2` fans out to both. Commit 6 (contract tests + real end-to-end verify, final): new `TabletapWeb.ApiShape.assert_json_shape/2` (hand-rolled exact-key-set comparison, no snapshot library warranted) plus a genuinely executed scripted end-to-end test — login → menu → cart → wallet checkout → channel join → real `ChargeOrder` Oban job (mocked only at the WaafiPay boundary via `ProviderMock`) → real waiter-assignment algorithm → real state transitions → real served-scan API call, asserting the channel receives every one of `placed`/`accepted`/`preparing`/`ready`/`served` live. 961 tests green.
- **Phase 8, Feature 24 (TableTap Customer App) CLOSED:** staged 6 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. The first genuinely different kind of work this session — a real TypeScript/React Native (Expo) monorepo, not an Elixir feature. Real environment constraints checked directly up front: no physical device, no Android emulator image, no Xcode in this sandbox — Playwright's already-cached Chromium driving Expo's web target (`react-native-web`) is the honest substitute for interactive verification. Commit 1 (backend: 4 missing endpoints, `6e5a270`): table-QR resolution, call-waiter, rate-item, cross-venue history — thin wrappers over existing context functions, same shape as every Feature 23 controller. Commit 2 (monorepo scaffold, `721a393`): `apps/customer` (Expo SDK 57, TypeScript strict, `expo-router` added by hand since the blank template ships without it) + `packages/shared` (design tokens ported 1:1 from ui-tokens.md, typed `/api/v1` client, `phoenix`-client Channels wrapper). Commit 3 (scan/menu/cart, `b84a08b`): `expo-camera` QR scan, menu + modifier sheet + cart bar; new `TabletapWeb.CorsPlug` (Expo web target is a real browser, subject to CORS unlike a native app). Commit 4 (checkout/tracker, `9dbea36`): wallet checkout + live tracker mirroring `Public.OrderTrackerLive`'s states exactly; fixed the `phoenix` JS client silently failing to connect over an absolute `http://` URL (needs explicit `ws://` rewrite). Commit 5 (history/ratings/push, `c2a85dc`): magic-link login via the `tabletap://` deep link, order history, post-order star ratings, push opt-in (gracefully degrades — no EAS project registered in this sandbox, so no real Expo push token can be minted). Commit 6 (final verification + EAS scaffolding, `8cd0b1f`): `eas.json` build profiles scaffolded (no real build attempted); full cohesive Playwright walkthrough (menu → cart → cash checkout → tracker) plus a real `accepted → preparing → ready → served` live-update sequence driven via the same named-node + remote-shell RPC technique Commit 4 established, confirmed arriving over the real Channel with zero reloads. 974 tests green throughout; `tsc --noEmit`/`eslint` clean on both `apps/customer` and `packages/shared`.

- **Phase 7, Feature 22 (Load & Security Pass) CLOSED — Phase 7 is now fully closed:** staged 3 commits, plan file at `.claude/plans/shiny-mixing-donut.md`. Commit 1 (tenant-isolation audit): read all 77 `skip_org_id: true` occurrences individually — **no violation found**, every real usage is the documented exception list or an unavoidable `Org`/`User` query. Added targeted cross-tenant LiveView tests across one representative surface per role (`Waiter.QueueLive`, `Kitchen.BoardLive`, `Cashier.PosLive`, `Manager.OrdersLive` — the latter had zero test coverage at all before this, flagged not backfilled). Found a genuinely interesting, non-obvious scenario worth its own test: order numbers are sequential *per venue*, so two fresh tenants both landing on order #1 is the realistic norm — confirmed a cashier's number lookup always resolves to their own org's order, never leaks the other tenant's (using two very differently-priced items so a real leak would be visually distinguishable, since the correct behavior looks identical to "not found" otherwise). `Admin.TenantsLive` already had a test explicitly confirming its cross-org visibility is intentional design, not a leak. Commit 2 (rate limiting): `RateLimiter.check/1` gained optional `opts` (`:max`/`:window_ms`, default unchanged) so different actions can carry different budgets; new `client_ip_from_conn/1` for plain `Plug.Conn` callers (no LiveView socket). Three new call sites, all deliberately generous: `/t/:qr_token` (30/60s, new `:rate_limited` render clause + template mirroring the controller's own existing honest `:not_found` page), `Public.MenuLive`'s `add_to_cart` (60/60s) and checkout (15/60s, tightest — real financial consequences per attempt). Fails open if `client_ip` is somehow unset. Commit 3 (load test, final): a real, executed (not theorized) 50-cart + 200-tracker `Phoenix.LiveViewTest` load test — full narrative of the three real architectural blockers hit (lazy_html test-only dep, LiveView test helpers barred from spawned processes, Ecto Sandbox's per-process connection pinning even in `:auto` mode) is in `context/progress-tracker.md`'s Feature 22 entry and in Problems solved below. Real numbers captured: 156/250 sessions sustained at `pool_size=40` before pool exhaustion, +56 MB BEAM memory (~367 KB/successful session), checkout latency avg 12.9ms/p95 23ms while under load — genuinely reassuring on latency, genuinely limiting on connection-pool sizing. No app code changed; verification was the captured numbers themselves, not a new automated test.

## Decisions made

All build decisions are recorded in `context/progress-tracker.md` and `context/design-qa.md` (Q1–Q63) — those are the source of truth, not this file. Feature 18's own cross-cutting decisions (payment-mix scope, food-cost-estimate vs accurate figure, rollups-never-closed, staff_metrics scope) live in `Tabletap.Analytics`'s own moduledoc — read that first before touching any Analytics function.

## Problems solved

Per-feature bug narratives are in the tracker. Durable environment/infra lessons worth keeping here:

- Playwright IS usable in this environment: `npm install playwright` in a scratch dir picks up the cached Chromium at `~/.cache/ms-playwright` (no download). `/dev/mailbox` (Swoosh local adapter) is real and scriptable too.
- `Repo.prepare_query/3` injects `org_id` into **every** query it runs through the tenant-enforced Repo, including preload sub-queries — `preload: :user` off a tenant-scoped query crashes because `users` has no `org_id` column. Fix: two explicit queries, zipped together.
- **`Repo.put_org_id/1` is ambient process state, not tied to whichever `%Scope{}` you're holding.** Any helper doing Ordering-context reads/writes should re-assert `Repo.put_org_id(scope.org.id)` as its own first line.
- Cross-tenant reads/writes belong in whichever context already owns the underlying table — loop `Tenants.list_org_ids/0` + `Repo.put_org_id/1` per org (`Workers.SweepAbandonedCarts`'s shape). Never reach for `skip_org_id: true` outside `Accounts`/`Tenants`/platform-admin.
- A `phx-trigger-action` form submit is a no-op if clicked before the LiveSocket JS attaches — add `page.waitForLoadState("networkidle")` first in Playwright verify scripts.
- Always boot the real dev server after Repo/Oban/supervision changes; `Oban testing: :manual` hides boot loops from the test suite.
- Root layout has no sitewide `<.flash_group>` — a controller `put_flash` + `redirect` is silently invisible.
- Never hard-match `Decimal.parse/1` on raw user-typed input — returns `:error` for anything non-numeric.
- **`mix compile`/`mix test` can silently start compiling a whole Erlang toolchain from source** if `.tool-versions` names a version `mise` doesn't have cached. Check `asdf list erlang` first; prefix commands with `PATH="/home/rirash/.asdf/shims:$PATH"`.
- `<.money>` with no `locale=` falls back to the app default (`:en`) — differs visibly from a venue-scoped call on the same page. Know which one a row uses before asserting exact formatted text.
- **`timestamps(type: :utc_datetime)` (second precision) means two rows in the same wall-clock second are a real tie for `order_by: [desc: :inserted_at]`.** Always add a secondary sort key. When a test also needs to assert exact order, sort its own "expected" list by the *identical* composite key (`{DateTime.to_unix(inserted_at), id}`, not just `id` alone) — Feature 17's own tie-break test had exactly this bug (assumed a tie, broke when timestamps happened to differ), fixed in Feature 18 part 4a.
- Elixir requires every `def handle_event/3` (or same-name/arity function) clause to be contiguous — a `defp` between two clauses is a compile error under `--warnings-as-errors`. Recurred 3+ times this project; always append new private helpers *after* the full clause group.
- A brand-new venue has `charges_enabled: false` and `pay_at_counter_enabled: false` — a verify script must flip `pay_at_counter_enabled` on before a guest can check out.
- Phoenix's `<.button>` component doesn't render `type="submit"` — a raw `<button type="submit">` does. Check which one a template uses before writing a Playwright/test selector.
- **A raw `Date`/`DateTime` struct can't go into a `~p"...?#{[key: value]}"` verified-route query string** — `Phoenix.Param` has no impl for it, raises `ArgumentError` ("structs expect an :id key"). Always `Date.to_string/1` first. Bit every new Analytics LiveView's CSV-export link and range picker this session.
- `Enum.sort_by/3`'s `{:asc/:desc, Module}` sorter works for any module exposing `compare/2` returning `:lt/:eq/:gt` — confirmed both `Money.compare/2` (same-currency only) and `Decimal.compare/2` satisfy this, so `{:desc, Money}`/`{:desc, Decimal}` are safe, idiomatic sorters throughout Feature 18's Analytics code instead of hand-rolled comparators.
- **This session hit a live concurrent-edit collision**: another session was working on Feature 19 (Subscriptions & Platform Admin) in the *same checkout* at the same time. Several of my own uncommitted files got silently reverted to their last-committed state mid-session (lost ~30 min of Analytics/DashboardLive work, had to redo from conversation history). Lesson: if a file you just wrote comes back reading like an older version, don't assume you misremembered — check `git status`/`git diff` immediately, since concurrent writers to the same working tree are a real failure mode here, not a hypothetical. When it happens, stop and ask the user rather than guessing whether to overwrite.
- When two sessions share a working tree and a shared file (router.ex, layouts.ex) ends up with both sessions' uncommitted hunks mixed together, ask the user how to handle committing it (bundle together vs. hold out) rather than guessing — this user's call was "bundle together, don't lose either session's work."
- **Two Ecto schemas can't reference each other at compile time even indirectly** — `Reports.subscribe/3` pattern-matches `%ReportSubscription{}` (a struct literal), and `ReportSubscription`'s own schema tried `Ecto.Enum, values: Reports.report_types()` (a function call into `Reports`), and the two files deadlocked compiling each other. Fix: inline the literal atom list in the schema instead of calling back into the context module — a small, deliberate duplication rather than a real coupling.
- **`Repo.prepare_query/3`'s auto-injected `org_id` filter applies to *every* nested preload query, including ones against schemas that were never meant to be tenant-scoped** (`Org` itself — no `org_id` column, an org *is* the tenant; `User` — no `org_id` column, a user can span orgs via multiple memberships). Any preload or join that touches `Org`/`User` from tenant-scoped code needs its own two-query fetch with explicit `skip_org_id: true` (`Tenants.list_memberships/2` is the canonical example — read its own comment before reinventing this).
- **Swoosh test mailbox is FIFO and un-filtered at receive time** — `assert_email_sent(to: ...)` is `assert_received {:email, email}` (grabs the oldest `{:email, _}` message) *then* checks params against whichever one it got. A test fixture that itself sends email (`AccountsFixtures.user_fixture/1` does, via `deliver_login_instructions`) pollutes the mailbox ahead of the email you actually want to assert on. Drain the mailbox (`receive do {:email, _} -> ... after 0 -> :ok end`, looped) right before triggering the send you're testing.
- **`Swoosh.Attachment.new({:data, binary}, filename: ..., content_type: ...)` attaches in-memory content** (no temp file needed) — confirmed via `deps/swoosh/lib/swoosh/attachment.ex`, used for emailing a CSV built in-process rather than round-tripping through disk or an authenticated download link.
- **This environment's test DB connection pool (`pool_size: schedulers_online() * 2` = 8 on this 4-core box) is smaller than some existing concurrency-hammer tests' own concurrent-task count (e.g. a "10 concurrent checkouts" test in `checkout_test.exs`).** Intermittent `DBConnection.ConnectionError`/"could not checkout the connection" failures in `checkout_test.exs`/`tenants_test.exs` are pre-existing environmental flakiness from this mismatch, not a real logic bug — reproduced across several `mix test` runs, confirmed unrelated to files touched by Feature 20 Commit 4. If this needs fixing, the target is `config/test.exs`'s `pool_size` or those tests' own concurrency count, not anything in Feature 20.
- **`phx-hook` only initializes on elements inside a LiveView's own socket-managed DOM subtree.** `root.html.heex` is the static shell around `{@inner_content}` — a `phx-hook` placed directly in the root layout (outside any `<.live_view>` root) never fires, since LiveView only discovers/binds hooks while diffing a View's own container. Anything living in the root layout itself (offline banner, install-prompt button) needs plain vanilla JS wired at page load in `app.js` instead, operating on the element by id — same shape as the pre-existing theme-toggle `<script>` and topbar wiring already in that file.
- **Manually verifying cross-node `Phoenix.PubSub` locally (two `elixir --sname` BEAM nodes) is genuinely doable and worth doing — but budget a generous receive-timeout (60–90s+) on the "listener" node, not a tight one (~15–30s).** The actual `Node.connect/1` + `:pg` group sync always completed in well under a second every time it was checked directly (`:pg.get_members/2`) — every "no message received" failure was purely because composing/running the second node's script (via several sequential tool calls: write file, sleep, run) ate more wall-clock time than the listener's own short timeout, not a real clustering defect. Confirmed by directly RPC-querying the listener node's own `Node.list()`/`:pg.get_members/2` mid-test — cluster membership was always correct; only the timing of the test harness itself was wrong.
- **A second Ecto repo pointed at the same physical database (`Tabletap.ObanRepo`, kept separate from `Tabletap.Repo` specifically so Oban's own bookkeeping tables stay outside tenant `org_id` scoping) needs its OWN `Ecto.Adapters.SQL.Sandbox.mode(_, :manual)` call in `test_helper.exs` and its own `Sandbox.start_owner!/2` in `DataCase` — the sandbox checkout for one repo module does not cover a second repo module even against the identical database.** Without it, every `Oban.insert/1` in every test ever run commits for real instead of rolling back at test end — silent, invisible until something asserts `refute_enqueued(worker: X)` with no `args:` filter (broad enough to catch stray rows from unrelated tests/runs). Found via 3375 accumulated stray rows in `tabletap_test.oban_jobs`. Any future second-repo addition in this codebase needs the same two-line treatment from day one.
- **Oban's `unique:` option has two easy-to-miss requirements, both found by a test that asserted the actual enqueued job's args (not just that `Oban.insert/1` returned `{:ok, _}`):** (1) `keys:` must be a list of **atoms** even when the worker's own `args` map uses string keys throughout (every worker in this codebase does) — passing string keys doesn't warn, it makes the changeset invalid and `Oban.insert/1` returns `{:error, changeset}`. (2) `fields:` must explicitly include `:worker` if you also list `:args` — omitting it means Oban compares args *across every worker*, so a new job can get silently treated as a duplicate of a same-shaped-args job belonging to a completely different worker (`SendPush` colliding with an unrelated `AssignWaiter` job that happened to share an `order_id` key, both because `Ordering`'s own job-arg convention of `{"order_id" => ..., "org_id" => ...}` is reused loosely across several different workers).
- **A named GenServer's own ETS table (`RateLimiter`, and now `Payments.GatewayHealth`) is process-independent shared state across the whole test run — asserting on its cumulative value from inside an `async: true` test file is racy against every other concurrently-running async test that also mutates it.** ExUnit runs the whole `async: true` pool concurrently and only runs `async: false` modules afterward (never interleaved), so the fix is a small, dedicated `async: false` test file that resets the shared state in its own `setup` and asserts on it directly — never bolted onto an existing `async: true` file. `test/tabletap/payments/gateway_health_test.exs`/`gateway_health_integration_test.exs`/`test/tabletap_web/live/manager/gateway_degraded_banner_test.exs` are the pattern to copy for any future shared-ETS-backed feature. **By contrast, DB-backed state (Ecto Sandbox) genuinely is safe under `async: true`** — each test's own sandboxed transaction is isolated from every other test's, so a shared unique slug/constraint across concurrent tests never actually collides (`test/tabletap/ops_test.exs`, `test/tabletap_web/telemetry_test.exs`'s Oban-queue-depth test). Don't reflexively reach for `async: false` for every "shared resource" concern — the real dividing line is process-independent memory (ETS/GenServer state) vs. sandboxed DB state.
- **`Repo.get_by(Schema, key: val, skip_org_id: true)` silently does NOT do what it looks like — `skip_org_id: true` gets folded into the *clauses* keyword list (the second positional arg), never reaching `get_by/3`'s separate third `opts` argument, so the tenant-scope guard still fires.** Every correct call site in this codebase already wraps clauses in an explicit list: `Repo.get_by(Schema, [key: val], skip_org_id: true)` (see `Accounts.get_user_by_email/1`). Caught building `Tabletap.Ops` (Feature 21) by actually hitting the endpoint live rather than trusting compile success.
- **`Repo.preload(struct, :assoc)` on any association pointing at `Org` or `User` breaks the instant ambient `Repo.put_org_id/1` is set** — `Repo.prepare_query/3` injects `org_id` into preload's own internally-generated query, and neither table has that column (an org *is* the tenant; a user can span orgs). This was already documented in `Tenants.list_memberships/2`'s own comment from an earlier feature, and still bit `Tabletap.Ops.check_order_flow/0` fresh this session — the fix is the same every time: a plain two-query fetch (`Repo.one(from o in Org, where: o.id == ^id, skip_org_id: true)`) and manually attach it (`%{struct | assoc: result}`), never `Repo.preload/2,3` for an `Org`/`User` association from tenant-scoped code.
- **When writing cross-tenant tests that switch `Repo.put_org_id/1` between two orgs in the same test process, always set it back explicitly before creating "my own org"'s fixtures** — it does not reset itself. Cost real debugging time twice in the same commit (Feature 22): once produced a `BadMapError` red herring that looked tenant-related but was actually just destructuring `{order, order_item}` (a test helper's real tuple return, per every *other* caller of it in the same file) as if it were a bare struct; the other produced `Ordering.checkout/2`'s `cart` argument silently being `nil` because `add_to_cart`'s own ambient-scoped re-fetch found nothing under the wrong org. Neither was a real app bug — both were the test's own ambient scope pointed at the wrong org at the moment a fixture was built.
- **A "does org B ever see org A's data" test needs org A and org B's fixtures to be distinguishable in the assertion, not just present** — same-shaped fixtures (identical item name/price across both orgs) make a real leak and correct isolation render identical HTML, so the test can't actually tell them apart. Use a deliberately different, identifying value (a different price, in the POS order-number-collision test) so a real leak would visibly surface in the rendered output instead of passing either way.
- **`Public.MenuLive`'s checkout form (and its `@checkout_error` text) only renders at all when `@charges_enabled or @pay_at_counter_enabled`** — a brand-new venue fixture has neither, so a test that never enables one of the two will see `checkout_error` correctly *set* on the socket's own assigns (confirmed via `:sys.get_state`) while the rendered HTML never shows it, since the whole `<form>` is conditionally absent. Always `Tenants.set_pay_at_counter_enabled/3` (or enable wallet) before writing a MenuLive checkout-path test that asserts on visible error text, not just the assign.
- **`Phoenix.LiveViewTest.live/2` (and everything downstream of it — `render_click`/`render_submit`) can only ever be called from the literal ExUnit test process, never a spawned `Task`/`Task.async_stream`, even inside a real `ExUnit.Case` test run via `mix test`.** Confirmed by reading `phoenix_live_view`'s own `fetch_test_supervisor!/0`, which calls `ExUnit.fetch_test_supervisor()` — registered only against the test process, never propagated to a Task it spawns. A concurrent LiveView-session load test therefore cannot use `Task.async_stream` to open sessions in parallel; establish them sequentially from the one test process instead. This is sufficient when the actual measurement goal is "N sessions simultaneously *alive*," not "N sessions established at the same literal millisecond." Also: `lazy_html` (LiveViewTest's DOM-diffing dep) is `only: :test` in `mix.exs` — any such script must run via `MIX_ENV=test mix test <script>`, not plain `mix run`.
- **`Ecto.Adapters.SQL.Sandbox.mode(Repo, :auto)` does NOT bypass the Sandbox's own connection-ownership-tracking proxy — it only changes commit-vs-rollback behavior.** Every connection still routes through Sandbox's ownership wrapper, which has a hardcoded 120-second per-owner `:ownership_timeout` (visible in the crash as `Postgrex.Protocol disconnected... owned the connection for longer than 120000ms`) and appears to pin one proxy-tracked connection per *living* process rather than releasing it per-query the way a raw production DBConnection pool does. Practical effect: N concurrently-alive processes each touching the DB even once can exhaust a Sandboxed pool of size much smaller than N, in a way that would never happen against a real (non-Sandboxed) production Repo. If a load test genuinely needs many concurrently-alive DB-backed processes, either bump `pool_size` for the exercise (temporarily, reverted after — and watch out for the local Postgres server's own `max_connections` global ceiling, default 100, which a naively large `pool_size` blows straight through and crashes app boot with `too_many_connections`) or run outside the Sandbox entirely against a real, non-test Repo config.
- **A JSON API (or a `Phoenix.Channel` topic) is the first surface in this codebase genuinely exposed to fully arbitrary client-supplied ids.** Every existing LiveView route only ever receives an id the app itself generated in a server-rendered link, so this never came up before Feature 23. A malformed `:binary_id` (UUID) string reaching a plain Ecto query (`where: x.id == ^id`) raises `Ecto.Query.CastError` — an unhandled 500, not a graceful 404/rejection. Fix: cast first, always, on any param flowing into a `get_*(scope, id)`-shaped call from a controller or channel `join/3` — `TabletapWeb.Api.Params.cast_uuid/1` is the reusable guard, `{:ok, id} <- cast_uuid(param)` in a `with`, else-branch to a 404/`{:error, %{reason: "unauthorized"}}`. Hit this same bug twice in one feature (REST params in Commit 2, channel topics in Commit 3) — expect it again anywhere a new client-facing surface accepts an id.
- **A membership-based authorization query that filters on `venue_id == given_id` alone silently excludes every owner** — an owner's own `Membership` row is org-wide (`venue_id: nil`, matching `Tenants.build_scope/2`'s own established handling), not tied to any specific venue. The naive one-line fix ("or `venue_id` is nil") is a *worse* bug: it lets any org's owner authorize into any other org's venue-scoped resource, since a bare `is_nil(venue_id)` check on its own proves nothing about which org that membership belongs to. The correct fix joins the target venue and only accepts a nil-`venue_id` membership when that membership's own `org_id` matches the venue's `org_id` (`Tenants.get_active_membership_for_user_and_venue/2` is the reference implementation). Any future venue-scoped authorization check that isn't already running under an ambient `Repo.put_org_id/1` (i.e., cross-tenant `skip_org_id: true` lookups — exactly the shape a `Phoenix.Channel.join/3` or a bearer-token API controller needs, since neither has session-established tenant scope yet) needs this same owner-aware join, not just a `venue_id` equality check.
- **`Phoenix.Channel.join/3` runs in its own freshly-spawned process, separate from both the connecting Socket's process and the caller/test process** — `Repo.put_org_id/1` (process-dictionary state) set inside `join/3` only affects that channel process, exactly like a LiveView's own `mount/3` needing its own `Repo.put_org_id/1` call. `Phoenix.PubSub.subscribe/2` called inside `join/3`, before returning `{:ok, socket}`, is the correct, standard idiom — `Phoenix.ChannelTest.subscribe_and_join/3` is synchronous and only returns after `join/3` completes, so a broadcast sent immediately after in the test is safe by construction. One genuinely rare flake was observed (a single failure in ~200 full-suite `mix test` runs this session, never reproduced in isolation or on immediate retry) consistent with a `:pg`-based PubSub subscription-registration/broadcast timing race under heavy scheduler contention — same category as this project's other documented full-suite-only flakes (`checkout_test.exs`'s concurrent-checkout test), not a code defect.
- **`Oban.Testing.perform_job/2` runs a worker's `perform/1` synchronously in the calling process** — no real Oban dequeue, no separate process, so `Mox.expect/stub` set up in the same test process is directly visible with zero `Mox.allow/3` ceremony, and `Repo.put_org_id/1` calls inside the worker are scoped to that same test process too. This makes driving a full "checkout → mocked wallet charge → order placed" flow in one ExUnit test straightforward: build the payment via the real `Payments.charge_order/3` call, then `perform_job(ChargeOrder, %{"payment_id" => ..., "org_id" => ..., "wallet_msisdn" => ...})` with `ProviderMock` stubbed — `test/tabletap/payments/workers/charge_order_test.exs` and Feature 23 Commit 6's own end-to-end test both use this pattern.
- **`Ordering.assign_waiter/3`'s solo-on-shift-waiter branch auto-accepts the order in the same call** (`:placed -> :accepted`, no 90s claim-board window) — a test that wants to observe a real `:placed`-and-assigned-but-not-yet-accepted order needs *two* on-shift waiters (the "many candidates" load-balancing branch), not one. `assign_waiter/3`'s third arg is an injectable `alive?` function (`fn _venue_id, _membership_id -> true end`) — the same pattern `assignment_test.exs` already established — sidesteps needing real `TabletapWeb.Presence` tracking in a test; the real `AssignWaiter` Oban worker calls `assign_waiter/3` *without* this override, so it depends on genuine Presence, which is why a broader integration test should call `assign_waiter/3` directly rather than going through the worker.
- **`org_fixture/1`'s returned `%Venue{}` does NOT have `:org` preloaded** — building `%Scope{org: venue.org, venue: venue, ...}` compiles and even assigns fine (a `#Ecto.Association.NotLoaded<>` struct is a legal value to store), but crashes the moment anything downstream dereferences `.org.id` (e.g. `Ordering.add_to_cart/7`'s `insert_cart/4`). `org_fixture/1` already returns `org` directly (`%{org: org, venue: venue, user: user, membership: membership}`) — always build guest/staff scopes from that raw `org`, never `venue.org`, in any test that actually exercises a context function with the scope (as opposed to just holding it for a `Tenants.*` call that never touches `.org`).
- **`jason`'s `Jason.Encoder` impl for `Decimal` is compile-time conditional** (`if Code.ensure_loaded?(Decimal) do` in `jason`'s own source) — it only exists if `Decimal` was already loaded when `jason` itself compiled. A stale `_build/dev` that predates some later dependency reordering can silently be missing this impl even though `_build/test` has it, crashing on encoding any `Money`/`Decimal`-bearing response specifically under `mix run`/dev. Fix: `mix deps.compile jason --force`. Not an application bug, and a real `mix release` (full dependency-graph-ordered compile) wouldn't hit it — but worth checking first if a `Money` JSON response crashes only in one environment and not another. Confirmed empirically while debugging this: `Money`'s `amount` serializes as a JSON *string* (`"5.00"`), never a bare float.
- **A browser-based client (Expo's web target, or any `react-native-web` build) is subject to CORS; a real native app on a phone never is** — it never sends an `Origin` header at all. Don't assume "the API already works for the mobile app" transfers to "the API works when driven through a browser-based stand-in for the mobile app" in an environment with no physical device/emulator; a CORS preflight has to be answered *before* the router even matches a route (no route is registered for `OPTIONS`), so a CORS plug needs to sit in the Endpoint ahead of the Router, not inside any pipeline.
- **The `phoenix` npm client only auto-derives a `ws://`/`wss://` scheme when given a *relative* socket path** (confirmed by reading its own `endPointURL()` source) — pass it an absolute `http://` URL and it hands that straight to the `WebSocket` constructor as-is, which fails to connect with **no visible error, not even in the join callback** (the failure happens below that). Always rewrite `http`→`ws` explicitly (`baseUrl.replace(/^http/, "ws")`) before constructing a `Socket` from a fully-qualified base URL.
- **A plain `mix run` script boots its own separate BEAM VM with its own separate `Phoenix.PubSub` instance** — broadcasting from it never reaches an already-running `mix phx.server` node, silently. To manually drive/observe real-time state from outside a running dev server (watching a live LiveView/Channel tab update from a script), restart the server as a named node (`elixir --sname phxdev --cookie <secret> -S mix phx.server`) and drive it via a remote-shell RPC (`Node.connect/1` + `:rpc.call(node, Code, :eval_string, [...])` from a small connector script that itself calls `Node.start/2` with its own distinct sname) — this is the only way a script and the live dev server end up on the same PubSub. Give any Playwright script that's watching for the resulting live update a generously long polling window (minutes, not ~60s) — debugging/fixing the RPC script itself reliably eats more wall-clock time than expected, and a polling script that already exited before the real transition lands looks exactly like "no live update happened" when it's actually just a timing/script-lifetime bug.
- **A serializer that's "complete" for one caller can still be missing a field a different caller needs** — `Serializers.order_item/3` exposed `menu_item_id` but never the order item's own `id`, invisible until a new caller (the rating endpoint, which addresses a specific line item) needed it. When adding a new client of an existing serializer, check what identifiers the new endpoint actually needs before assuming the existing shape already covers it.
- **`Ordering.accept_order/2` requires the calling membership to already be the order's assigned `waiter_membership_id`** — it is not a generic "any staff member can accept a placed order" call, and an owner/manager scope with no prior assignment correctly gets `{:error, :not_yours}`, not a silent success. `Ordering.claim_order/2` (the claim-board path, atomic `UPDATE ... WHERE waiter_membership_id IS NULL`) is the right call for a `:placed` order nobody's claimed yet — matches what a real staff member with no prior claim would actually do. Relevant any time a script or test needs to drive an order forward without an existing waiter assignment already in place.
- **`push_navigate/2` ends a `Phoenix.LiveViewTest` view's usable life** — after a successful checkout, `Public.MenuLive` navigates to `Public.OrderTrackerLive`; continuing to `render_click`/`render_submit` against the *same* `lv` handle afterward fails with "process is not alive." A test that needs to fire the same event N times (e.g. hammering a rate limit) must make every attempt fail for a *reason unrelated to what's being tested* (here: an empty/nonexistent cart crashes `Ordering.checkout/2` outright — used a paused venue instead, which fails gracefully every time without ever succeeding or navigating away) so the view survives across all N attempts.

## Current state

- **Phases 1–6 CLOSED.** Feature 18 commits: foundation `97febd0`, Today screen `2ad60c1`, Revenue & Sales + Menu Performance `024e181`, rich Feedback + Customers `738e064`, Staff & Work + Inventory & Cost `0628638`, Org View `e15a467`, Report Center `3fc68b4` (13 reports, CSV, scheduled email). 747 tests green via `mix test` at Commit 6 (`mix precommit`'s credo step halts on the pre-existing `Tabletap.Plans` alias-ordering finding in `payments.ex`, belonging to the concurrent Feature 19 work described below — run `mix format --check-formatted` + `mix compile --warnings-as-errors` + `mix test` separately when that finding is present, same workaround used throughout this feature).
- **No browser-based verification for Commit 6** — no Playwright/`chromium-cli` available in this environment this session (unlike earlier in the project where `npm install playwright` picked up a cached Chromium — that cache may no longer be present, wasn't re-checked). Relied on the LiveView test suite instead. Worth re-checking whether Playwright is available before assuming it isn't next time.
- **The dev Postgres database (`tabletap_dev`) was found empty at the start of this session** — only `tabletap_test` existed, `tabletap_dev` was gone. Almost certainly fallout from the previous session's Docker port-conflict fix (`docker compose rm -f db && docker compose up -d db` — a full container recreate, not just restart). Fixed with a plain `mix ecto.create && mix ecto.migrate` (dev data is disposable, wasn't seeded with anything precious). If this recurs, check `docker volume ls` for whether the compose file uses a named volume that should have survived a container recreate — it may not, which would explain the data loss.
- **Feature 19 Commit 1 landed (feature-gating retrofit)**: audited the concurrent-session `Plans`/`PlanHooks` scaffolding against pricing.md/build-plan.md and found it correct — reused as-is. Wired `PlanHooks :report_center` into a new `live_session :manager_report_center` for `/reports`, `PlanHooks :org_comparison` into a new `live_session :owner_org_comparison` for `/analytics/venues` (stacks with `:require_owner`), and a new `UserAuth.require_report_center_feature/2` for `/reports.csv`. `/analytics/revenue.csv`/`/analytics/menu-performance.csv` deliberately stay ungated (pricing.md only tiers the Report Center + org comparison, not the individual always-available screens). Nav links in `layouts.ex` gained the matching `Plans.feature_enabled?/2` guards. Wrote the first tests ever for this gating mechanism (none existed for `:inventory` either). Committed as `465da06`.
- **Feature 19 Commit 2 landed (billing screen + trial banner + add-venue)**: `Manager.BillingLive` (`/settings/billing`), `Tenants.create_venue/2` (the app's first self-serve second-venue path, blocked at the plan's `venue_cap`), `Tenants.change_plan/2` (upgrade unrestricted, downgrade blocked over cap per Q48), `Payments.unsettled_platform_fees_by_currency/1` (grouped per currency, never summed — Q53), and a shared `<.trial_banner org={}>` in `CoreComponents` (needed `use TabletapWeb, :verified_routes` added to that module — it didn't have `~p` before). Committed as `c692fa6`.
- **Feature 19 Commit 3 landed (billing engine)**: new `invoices` table (`pending`-row-first idempotency, unique `(org_id, period_start)`) + `orgs.billing_wallet_msisdn`; `platform_fee_ledger.invoice_id` upgraded from a bare uuid to a real composite FK. `Tabletap.Billing.collect_invoice/1` does the actual `WaafiPay.charge/2` against `Payments.platform_credentials/0` (new — reads `config :tabletap, :waafipay`'s `platform_*` keys, which only existed in `config/runtime.exs`'s prod-only block before this — had to add dev/test placeholder values or `Keyword.fetch!` crashed outside prod). Billing period: 30 days, billed **in advance**, first period starts at `trial_ends_at` (not signup date — a trialing org's very first bill has to land right when the free ride ends, not 30 days after signup, or a 14-day trial would get a free extra 16 days). State machine: success → `:active`; first miss → `:past_due` (architecture.md is explicit ordering keeps working here); second consecutive miss → `:canceled` (a documented judgment call — no explicit grace-day count exists in the spec). Trial-ran-out-with-no-wallet → straight to `:canceled`, no `past_due` step (Q29). `CoreComponents.trial_banner/1` → `subscription_banner/1` (4 states), moved into `Layouts.manager` so it's automatic on every back-office page — removed the now-redundant explicit calls from `DashboardLive`/`BillingLive`. `Public.MenuLive` gained a dedicated `:unavailable` render clause (multi-clause `render/1`, matching `ReportsLive.report_body/1`'s established pattern) — a canceled org's venue shows ONLY the unavailable notice, not the menu with a banner on top (unlike `:paused`/`:closed`, which stay browsable). Nightly cross-tenant `Billing.Workers.CollectMonthlyInvoices` dispatches per org's status; runs at a fixed off-peak UTC cron rather than per-venue cutoff math (same simplification `DailyRollup` already established). Committed as `56aed9f`.
- **Feature 19 Commit 4 landed (platform admin panel)**: `users.platform_admin` boolean (migration, default false, no self-serve way to set it anywhere in the app). New `TabletapWeb.AdminAuth` on_mount (`current_scope.user.platform_admin`, not a role check — an admin isn't a tenant member). New `Tabletap.Admin` context — the first module outside `Accounts`/`Tenants` to legitimately use `skip_org_id: true` (code-standards.md's own "platform-admin code" exception, confirmed by re-reading `Repo`'s moduledoc closely): `list_tenants/0`, `get_tenant/1`, `cash_share_by_venue/1` (Q24), `list_invoices/1`. Two new LiveViews (`Admin.TenantsLive` at `/admin`, `Admin.TenantLive` at `/admin/tenants/:id`) using the plain `Layouts.app` shell, not `Layouts.manager` (an admin has no org/venue to drive that sidebar's venue switcher/nav). Strictly read-only — no form, no `handle_event` writes anywhere in this commit; "impersonation guard (read-only)" from build-plan.md was interpreted as "view-only through the panel," never a session-swap mechanism, as decided in the plan file before starting. Committed as `999da23`.
- **Feature 19 Commit 5 landed (GDPR data lifecycle, final commit — Feature 19 is now CLOSED)**: `Accounts.delete_account/1` (design-qa.md Q15) turned out to be nearly free — `orders.customer_user_id`/`item_ratings.customer_user_id` already carry `on_delete: :nilify_all` from their own Feature 16/17 migrations (those migrations' own comments anticipated exactly this), so deleting the `User` row *is* the anonymization mechanism; guarded against deleting an account that holds any staff membership (`memberships.user_id` is `on_delete: :delete_all`, would silently orphan a venue). Wired into `UserLive.Settings` as a "Danger zone," gated by the page's existing sudo-mode requirement — no extra confirmation needed. New `Tabletap.Offboarding` context: rather than surgically deleting ~30 child tables while keeping an org half-alive for 90 more days to satisfy Q54's 180-day payment-retention window, the 90-day mark copies out everything worth keeping (Q31's anonymized customer order archive, Q54's payment/dispute-evidence snapshot with its own `retain_until`) into two new flat/denormalized tables with **no FK back to the org**, then hard-deletes the entire org in one `Repo.delete!` — letting the schema's existing cascade FKs do the rest instead of hand-enumerating which tables to purge. Nightly `Offboarding.Workers.PurgeOffboardedTenants` finds orgs 90+ days into offboarding and dispute records past 180 days. `Tenants.export_org_data/1` (menu/orders/ingredients CSVs, reusing `Analytics.Reports.orders_report/3` + `Reports.Csv` for orders) + `Manager.OrgExportController` (zips them via Erlang's built-in `:zip.create/2`, no new dep needed) + `Tenants.initiate_offboarding/1`, all surfaced in `BillingLive`'s new danger zone. Downgrade-side venue-cap enforcement (Q48) was already fully covered by Commit 2's `Tenants.change_plan/2` — audited, confirmed, nothing added. Committed as `257c703`. 814/814 tests green, only the pre-existing unrelated `Tabletap.Plans` alias-ordering credo finding remains — same workaround (`mix format` + `mix compile --warnings-as-errors` + `mix test` separately) every commit this feature needed.
- **Feature 20 Commit 1 landed (Web Push infrastructure)**: `{:web_push_ex, "~> 0.2.0"}` was already a mix dep (unused, unconfigured) — generated real VAPID keys via `mix web_push_ex.vapid`, split config the same way as WaafiPay platform credentials (public key + subject unconditional in `config.exs`, private key per-env: dev/test hardcoded dev-only pair, `runtime.exs` prod reads `WEB_PUSH_VAPID_PRIVATE_KEY`). New `push_subscriptions` table + `Tabletap.Notifications.PushSubscription` — **not tenant-owned**, same `skip_org_id: true` reasoning `Accounts` already documents (a subscription belongs to a person, who can hold memberships across orgs). New `Tabletap.Notifications` context (`subscribe/2`, `unsubscribe/2`, `send_push/2`, `notify_user/2`, `vapid_public_key/0`). **Caught a real Ecto bug**: `Repo.insert(changeset, on_conflict: ..., conflict_target: ...)` without `returning: true` returns the struct's own *client-generated* id (binary_id autogenerate happens in Elixir before the insert) even when the upsert actually touched a *different*, pre-existing row — a re-subscribe test caught this immediately (asserted `first.id == second.id`, got two different ids). Fixed with `returning: true`, which forces Ecto to re-read the actual persisted row. Test HTTP stubbing: no existing Mox-style seam for a single function like this, so used Req's own built-in `Req.Test` (`plug: {Req.Test, Tabletap.Notifications}` sourced from a `config :tabletap, :web_push_req_options` application-config seam) instead of building one. Service worker at `priv/static/sw.js` (root-served — added `sw.js` to `TabletapWeb.static_paths/0` — a service worker's default scope is its own path and below, so it can't live under `/assets`). Shared `<.push_subscribe_button>` component + `push_subscription.js` hook wired onto `Waiter.QueueLive` and `Manager.DashboardLive`. Discovered `Waiter.QueueLive` had **zero existing test coverage** — flagged as a pre-existing gap, only added tests for what I touched (the push wiring), not a full backfill. 824/824 tests green, only the pre-existing unrelated `Tabletap.Plans` alias-ordering credo finding remains.
- **Feature 22 Commit 3 landed (load test, final — Feature 22 is now CLOSED, Phase 7 is fully closed)**: real, executed 50-cart + 200-tracker `Phoenix.LiveViewTest` load test — see Problems solved for the 3 architectural blockers hit and worked through. Real captured numbers: 156/250 sessions established at `pool_size=40` before Sandbox-proxy connection exhaustion (39/50 cart, 117/200 tracker), BEAM memory 115.67 MB → 171.57 MB (+56 MB, ~367 KB per successfully-alive session), process count 373 → 722 (+349), checkout latency while under load averaged 12.9ms (p95 23ms, 10-sample) — reassuringly close to idle-system latency, meaning the real bottleneck this exercise surfaces is DB-connection availability for *new* session establishment, not per-request query latency once seated. `config/test.exs`'s temporary `LOAD_TEST_POOL_SIZE` env-var hack was fully reverted (`git diff` confirmed clean) before committing. Committed as `98b7ef0`, hash filled in via `2b51f81`. 891/891 tests green (had to `MIX_ENV=test mix ecto.reset` once first — a killed/interrupted earlier load-test run left stray seeded orgs/users in `tabletap_test` that hadn't reached their own cleanup step, causing 5 unrelated-looking test failures that were actually just leftover data).
- **Feature 23 landed (API & Auth Hardening for Mobile) — Feature 23 is now CLOSED, Phase 8's foundation is in place**: full narrative is in "What was built" above and `context/progress-tracker.md`'s Feature 23 entry. Commits `b2b4e0e` (auth foundation), `32004b4` (customer API), `65818d9` (Channels), `4027ef2` (staff API), `7668e33` (Expo push), `a4830de` (contract tests + real end-to-end verify, final). 961/961 tests green — one confirmed-rare, environment-load-dependent PubSub timing flake observed exactly once across ~200 full-suite runs this session (see Problems solved), reproduced neither in isolation nor on immediate retry, not a code defect. `.tool-versions` and `context/build-plan.md`'s stray table-reformat remain the older, unrelated, never-committed noise from before this session — still untouched.
- **Feature 24 landed (TableTap Customer App) — Feature 24 is now CLOSED**: full narrative is in "What was built" above and `context/progress-tracker.md`'s Feature 24 entry. Commits `6e5a270` (backend endpoints), `721a393` (monorepo scaffold), `b84a08b` (scan/menu/cart + CorsPlug), `9dbea36` (checkout/tracker + ws:// fix), `c2a85dc` (history/ratings/push), `8cd0b1f` (EAS scaffolding + final verification). 974/974 tests green; `mix credo --strict` clean except the pre-existing, unrelated `Tabletap.Plans` alias-ordering finding in `payments.ex` (untouched by this feature, last touched in Feature 21). Genuinely out of scope for this sandbox, flagged not silently skipped: no physical device/emulator verification, no real EAS build/store submission, no real Expo push token, no cart/session persistence across app restarts.
- **Known open gaps (flagged in tracker):** staff-invite create/accept UI doesn't exist; `Public.MenuLive` unknown-slug invisible-flash bug; `venue.locale "so"` has no CLDR data; POS wallet-assisted sales don't set `cashier_membership_id`; `/me/history`'s monthly-spend row uses app-default locale (deliberate); Feature 18's own scope gaps (payment mix has no per-wallet-network split, per-item food cost is a recipe-base estimate not the accurate stock-movement figure, no escalation-rate metric, no per-person kitchen prep time, no "time it happened" on sold-out/86 events, no manual-serve-confirm count, no season/quarter grouping UI); Feature 19's own gap — push-token purge (Q15) was a documented no-op since no `push_tokens` table existed — **now resolved by Feature 20 Commit 1's `push_subscriptions` table** (`on_delete: :delete_all` on `user_id`, so `Accounts.delete_account/1` already purges it automatically, no extra code needed). `Waiter.QueueLive` had no test coverage before Feature 20 Commit 1 — still true beyond the push wiring itself. Feature 24's own scope limits: no `@react-native-async-storage/async-storage` (not on the approved package list yet, so cart/auth state is session-only); no real Expo push token possible in this sandbox (no EAS project registered). `.tool-versions` and `context/build-plan.md`'s stray table-reformat remain the older, unrelated, never-committed noise from before this session — still untouched.

## Next session starts with

**Feature 25 — TableTap Staff App** (Phase 8, the last feature in the plan — see `context/build-plan.md`'s own Feature 25 entry for scope). Waiter mode (shift toggle, assigned FIFO queue, claim board, accept/serve with in-app QR scan-to-serve, call-waiter alerts — push needs to wake a locked phone reliably, per build-plan.md "the reason this app exists"), owner mode (today's live revenue/orders over Channels, venue comparison, low-stock/delayed-order/subscription alerts), role detection at login with a mode switcher for users holding both roles. Builds against the same `/api/v1` + Channels surface Feature 23 laid down, and reuses the `packages/shared` monorepo scaffold (design tokens, typed API client, Channels wrapper) Feature 24 already built — a new `apps/staff` sibling to `apps/customer`, not a fresh monorepo. No plan file exists yet; read code-standards.md's own "Mobile Apps" section (approved packages: `expo`, `expo-camera`, `expo-notifications`, `phoenix`, `expo-router`, `zustand` — nothing else without updating that list first) before drafting one. This sandbox still has no physical device/emulator — expect the same Expo-web-via-Playwright verification substitute Feature 24 established, and the same cash/RPC-driven-transition workaround (not real wallet checkout) for any live-update check, since no real WaafiPay sandbox credentials exist here either.

## Open questions

- Sessions between 2026-07-08 and 2026-07-15 didn't save memory — assume the tracker is complete for that gap.
- Whether the concurrent Feature 19 session will resume, or whether the next session should just finish what its scaffolding started — check `git log`/`git status` at the start of the next session before assuming the working tree matches what's described here.
